# 机器替代 AI 替代表 · 三档名单(冻结件 · 第 2 步)
# ─────────────────────────────────────────────────────────────────────────────
# 权威正文 : ~/Obsidian/项目入口/laixin开发流水线/知识库/raw/提案-机器替代AI替代表-20260829.md §五
# 裁定入档 : 来信 KB 4-开发层/11C-圆桌/议题/议题-来信流水线系统性优化-20260828.md §九
# 拍板     : 创始人 2026-08-29 02:03:07 逐字「对,确定,就按这个」(看板 02:03 条,来源=创始人窗口)
# 落笔     : 11B 主持第六任 pingxia-36(= 方案窗口,注册表 01:36 正名同一角色)
#
# 判档三原则(⛔ 改写):
#   1. 档位由程序判 ⛔ 由提交者自称。
#   2. ⛔ 只看路径 —— 窄白名单 + 整片 diff 逐文件判、取最高档。
#   3. 无法识别 ⇒ 自动上浮 B(命中 C 关键词则 C)⛔ 降档;上浮原因写进事实单。
#
# 🔴 本文件自身的改动 = C 档(§五 逐字:「名单本身的改动 = C 档」)。
# 🔴 M1 转正前,依本文件产出的一切事实单一律标「影子运行」⛔ 算替代 ⛔ 删验收窗动作。
# ─────────────────────────────────────────────────────────────────────────────

[A_WHITELIST]
# 惰性件窄白名单:必须「不被任何 AI 或程序读取为指令」。宁可漏判成 B,⛔ 放宽。
# ⛔ 收录 AGENTS.md / skills/** / prompt / 角色卡 / 注册表 / README —— 那些是「改 AI 行为的文档」= B 档。
docs/**
LICENSE
**/过程档/**
**/换班快照/**
**/archive/**

[A_CONDITIONAL]
# 条件式 A:路径命中还不够,须同时满足 diff 形态条件(程序判 ⛔ 人判)。
# 语义:diff 落在 tests/ 内,且「只新增」——无删除行、无修改行(纯 + 无 -)。
tests/**  require=only_added_lines

[C_PATHS]
# 高风险路径:命中即 C。
**/deploy*.sh
**/deploy/**
.github/workflows/**
11b/risk-tiers.conf

[C_FUNCTIONS]
# 工具仓 bin/ 内的高风险函数区(按函数名维护;13 项均于 2026-08-29 02:0x 实测存在)。
# 语义:整片 diff 若触及这些函数的行范围 ⇒ C 档。
cmd_release
cmd_backup
cmd_install_backup
wd_loop
cmd_watchdog
cmd_keepalive
dispatch_keepalive_off
trust_nav_confirm
vwait_ready
cmd_account_switch
cmd_dispatch
cmd_merge_guard
ensure_headless_settings

[C_SYMBOLS]
# 权限禁令注入面(实测存在的禁令数组;改动即 C)。
VERIFY_DENY
DISPATCH_DENY
RELAY_DENY
RELAY_ONCE_DENY
TOOL_DENY

[C_KEYWORDS]
# 无法识别的路径若命中以下关键词 ⇒ 直接 C(⛔ 只上浮到 B)。
权限
身份
迁移
资金
生产
release
deploy
credential
token
secret
