# laixin-pipeline 维护守则(11B)

> 本仓是来信平台流水线的工具仓。维护权归**来信方案窗口**(单一提交者);其他窗口发现问题**报给方案窗口**,不直接提交。

## 提交纪律

1. **三步纪律**:改 → `bash tests/run.sh` 全绿 → commit。修 bug 必附绊线测试,fixture 要构造成**修复被回退时会变红**(递进 fixture 参见 `9c5f82c` 的两条)。
2. ⛔ **提交必须显式列文件(`git add <文件>…`),禁 `git add -A` / `git commit -a`** —— 多窗口共用本仓且无 worktree 隔离。2026-08-13 夜实撞:`b7d9b0e` 用 `add -A` 把另一窗口在途的 `bin/laixin-lane` 修复卷进了自己的提交;提交者自查只核了"对方未提交的文件还在",**没核自己提交里有没有外来 hunk** ⇒ 自查射程不够,结论(没卷到)是错的。**卷走别人在途改动与流水线里"两轨对写同一文件"同族。**
3. 提交前 `git status --short` 逐行认领:**说不出来历的改动不许进提交**;提交后 `git show <commit> -- <文件>` **逐 hunk 认领**。⛔ 只看 `--stat` 文件列表是无效判据——**共改同一文件时,文件出现在列表里毫不可疑**(实撞:`b7d9b0e` 提交者用 `--stat` 自查得出"没卷到",逐 hunk 一看该文件里只有两行是自己的)。反事实一问就塌:"假如真卷走了别人的改动,`--stat` 会不会照样只显示这几个文件?"——会,故必须看内容。

## 已知的双真相源(改一处必须同步另一处)

- 交班闸门线:`contrib-statusline.py` 的 `GATE_HARD/GATE_WARN` 与 `bin/laixin-lane` `cmd_ctx` 的 `70/55`(一 python 一 bash,权衡后接受,两边有互指注释)。

## 判据方向

- **事实优先于措辞**:执行总表的措辞有无穷变体(`design-ready` 撞 `ready`、"已交付并在验收中"顺带提及、排队行滞后)。凡从台账取"什么可发/什么已合入",一律**交叉比对片名出现在哪一节**,不靠子串匹配措辞下结论(同族修复:`aebd38a` 油表、`9c5f82c` 下一片提示)。
- 工具自身不许重蹈它要解决的毛病:拿不到数据显 `?` 不显 `0`,"空比空"的相等不是相等。

## ⚠️ shell 会静默改写你的内容(一晚四发,三个角色各中一次)

反引号被求值(`laixin-lane log`)/ heredoc 吞长脚本 / **管道退出码被最后一段覆盖**(`run.sh | grep` 使红测试静默入库,`af957f4` 记账)/ **中文 locale 下 bash 把紧跟变量的多字节字符卷进变量名**(`$commit——` 击杀 set -u,`af957f4` 修)。⇒ **凡经过 shell 的文本与判据,验证"到达的内容/真实的退出码",不验证"发送的内容"**;变量后紧跟 CJK/全角标点一律 `${var}`;提交门控用命令自身退出码,不用管道尾段的。
