# laixin-pipeline 维护守则(11B)

> 本仓是来信平台流水线的工具仓。**维护权与合并权归创始人窗口(11B 归口;2026-08-19 更新——原「维护权归来信方案窗口」写于 11B 尚由方案窗口兼管的时代,当日事实已分叉:全天提交均出自创始人窗口及其委派代理,relay 落卡亦带 commit 号,文字随事实更新)**。提交权责三层:
> ① **创始人窗口**:直接提交,或委派一次性工程代理(委派权单点,⛔ 多线并发委派——145 行事件案);
> ② **方案窗口(11B 主持)**:🔁 **2026-08-28 20:42:45 解除原限制**——原文「仅 `skills/` 规则类落卡可直接提交;⛔ 碰 `bin/`/`tests/`」**作废留档 ⛔ 删**。**依据=创始人当轮直令逐字**「**那不行,方案窗口有所有的权限,这个问题是你必须把限制你的权限解除**」(2026-08-28 20:3x,紧接主持报「AGENTS.md 不归我改、卡住 R4 落地」之后)。⇒ **方案窗口对本仓全面提交权**(`skills/` · `bin/` · `tests/` 皆可),**安全形态照旧**:不同文件 + 改完即提交不留脏树 + ⛔ 同文件并发。⚠️ **原限制的立条背景已消失**:它写于「11B 由派工窗口承载」的时代,而**派工窗口(dispatch)已于 2026-08-28 18:14:43 按创始人直令任期终结**。
> ②-bis **`opt-11b`(11B 工具控制面闭环责任席;R4 授权,2026-08-28 20:42:45 落,权威=议题档 §八.一.②)**:起窗显式钉 `--model claude-opus-5` ⛔ 吃全局默认;工具件派工 → 一次性工程席实现 → 独立验收 → **本席串行合并与本地 `laixin-lane release`**;**⛔ `git push`**(GitHub 由每小时 `backup` 兜)· **⛔ 碰产品仓** · **产品仓唯一派工权不动**。🔴 **⛔ 改本文件(AGENTS.md)与 `tests/` 中的权责绊线** —— 即 R4 硬条「⛔ 被授权方自合『授予自己权限』的改动」;⚠️ **如实标**:该硬条**当刻靠明文 ⛔ 结构**,机器绊线已登记为第一批件,**在它落地前这一层是自觉不是闸**;
> ②-ter **工具仓=直修直合,测试当闸(创始人改判 2026-08-28 22:05:10;权威记录=看板 08-28 22:05 创始人窗口条〔原话逐字〕;⚠️ 本段于 2026-08-29 13:18:59 由创始人窗口从看板逐字捡回——当晚写入本文件未提交、08-29 上午被覆盖,期间本文件与「已落权威位」同形,见看板同日条;②-bis 闭环席自此作废留档 ⛔ 删)**。六条:①谁有手谁改——主持 / opt-11b / 创始人窗口 / 一次性工程席皆可直修直合,⛔ 唯一漏斗;②片 ≤20 分钟开发量,超了=切大了先拆;③验收=机器三查约 5 分钟:全绿且拆掉修法会变红 · diff 只碰该碰的文件零越界 · 真跑一次读数能分成败;⛔ 读报告 ⛔ 核文书面 ⛔ 比哈希;④第二双眼只限改权限/禁令/合并发布机制/安全面/AGENTS.md;⑤并发护栏不变(同时 ≤2、改主文件与 tests ≤1、合并 release 串行、⛔ push、显式列文件);⑥⛔ 为工具仓的事开局/盲答/互评——试比商量便宜。**方向不变、形态改**:R4 §八 六批与 D1–D5 作为方向保留;执行形态由「派工→一次性席→独立验收→合并」改为上述直修直合;opt-11b 角色指令 §一「闭环席」与注册表行归主持同步改口径。
> ③ **lane Codex**:仅限经创始人窗口立项的片,可在本仓**开分支**开发(守本文件全部提交纪律),⛔ 直接提交 main;验收后由**创始人窗口**合并。
> 其余窗口发现问题报创始人窗口(11B 队列)。并发红线不变:⛔ 同文件并发 · ⛔ 脏树滞留。

## 原生执行内核边界(2026-08-24 创始人裁定)

1. **原生 CLI 是执行内核,不替换**:Claude Code、Codex、Kimi Code 必须继续由各自官方 CLI 进程执行;⛔ 用第三方 harness、模型 API 包装或 11B 自制 runtime 取代。Skill、MCP、Plugin、Hook、权限、认证与工具调用继续归原生 CLI 生态。
2. **11B 只做控制面**:只负责调度、记忆、客观状态观测、恢复与验收;工具选择和工具执行仍归原生 CLI。新增能力优先接原生生态,⛔ 在 11B 另建模型循环、工具执行器、Skill 市场、MCP 客户端或记忆数据库。
3. **官方机器接口优先,不足就保留原生窗口**:能用官方 JSON/RPC/ACP/stdio 接口就做薄适配;官方接口不能保持真实任务所需能力时,继续走原生 TUI,⛔ 仿造工具层补洞。适配层失败必须显式报错并可退回当前原生路径,不得让调度故障升级成执行能力丢失。

**失败样本与理由**:创始人实用结论=基于 DeerFlow 改造的 NextOS 留在本机但不可用,勉强使用时也难扩 Skill/MCP;第三方 runtime 会让非程序员无法判断是项目、工具还是适配层故障。完整讨论与 11C 评估入口见 `/Users/pingxia/Obsidian/项目入口/laixin开发流水线/知识库/wiki/Pi与DeepSeek-Harness移植评估-20260824.md`。改变以上三条属于架构变更,必须先获创始人确认。

## 提交纪律

1. **三步纪律**:改 → `bash tests/run.sh` 全绿 → commit。修 bug 必附绊线测试,fixture 要构造成**修复被回退时会变红**(递进 fixture 参见 `9c5f82c` 的两条)。
2. ⛔ **提交必须显式列文件(`git add <文件>…`),禁 `git add -A` / `git commit -a`** —— 多窗口共用本仓且无 worktree 隔离。2026-08-13 夜实撞:`b7d9b0e` 用 `add -A` 把另一窗口在途的 `bin/laixin-lane` 修复卷进了自己的提交;提交者自查只核了"对方未提交的文件还在",**没核自己提交里有没有外来 hunk** ⇒ 自查射程不够,结论(没卷到)是错的。**卷走别人在途改动与流水线里"两轨对写同一文件"同族。**
3. 提交前 `git status --short` 逐行认领:**说不出来历的改动不许进提交**;提交后 `git show <commit> -- <文件>` **逐 hunk 认领**。⛔ 只看 `--stat` 文件列表是无效判据——**共改同一文件时,文件出现在列表里毫不可疑**(实撞:`b7d9b0e` 提交者用 `--stat` 自查得出"没卷到",逐 hunk 一看该文件里只有两行是自己的)。反事实一问就塌:"假如真卷走了别人的改动,`--stat` 会不会照样只显示这几个文件?"——会,故必须看内容。

## 已知的双真相源(改一处必须同步另一处)

> **2026-08-29 收敛**:`contrib-statusline.py` 已改为**单点源=仓内这一份**,线上 `~/.claude-official/statusline.py` 是**指向发布版的软链**(`laixin-lane release` 随发布换链到 `~/.local/bin/laixin-statusline`)。此前它是仓外**独立副本**,而下面那条常量绊线只比对闸门线数值 ⇒ **文件级不同步抓不住**——当日实撞:两份**同时**缺 `import os`(绝对余量闸一次都没执行过)而常量完全一致、绊线全绿。现由 tests 的「状态栏单点源」一条钉住软链解析到当前发布版,**⛔ 不存在即跳过**。

- 交班闸门线:`contrib-statusline.py` 的 `GATE_HARD/GATE_WARN` 与 `bin/laixin-lane` `cmd_ctx` 的 `75/70`(一 python 一 bash,权衡后接受,两边有互指注释;**2026-08-22 起 tests 一条绊线钉住同步**——当日实撞:bash 侧 `07f59d2` 上调 65/75 而 contrib 副本仍 55/70、本条原文也还写 70/55)。

## 规则的生命周期(2026-08-19 立,创始人认可;复盘页 #55——立规流程反转+卸载通道)

1. **立规先问机器化**:失败样本到手,第一问是「可否直接生为机器闸(lint/断言/子命令)」——可则**跳过纪律条阶段**直接进 11B 工程队列,⛔ 先落纪律卡再排机器化(纪律条是中间态,而中间态会被读者当终态;当日实证:5 次「知道规则但没在当口触发」,病全在触发不在知识)。判不可机器化的(机器化边界=判据是否免语义)才落纪律卡。
2. **卸载通道与登记通道同权重**:每次清账批固定带一步「本批机器化了谁 ⇒ 对应纪律条删谁并 log 留痕」——准入规则的删除半边此前零执行(当日:机器化 15 条、删纪律 0 条),规则库只进不出=后任阅读税与触发失败面单调涨。
3. **跟踪数=规则机器化率**(机器断言拦截可外推,纪律拦截 ⛔ 外推——混计即高估防线覆盖率)。

## 操作卡防回胖闸门(2026-08-25 创始人要求固化;失败样本=pipeline 卡曾涨至 136991 B)

1. **卡只放每任必用的触发、最短操作、红线与权威指针**;当前状态进总表,事故史/证据进记录或复盘,规则正文进机制/SOP,⛔ 回抄进卡形成副本。
2. **新增先腾位置**:新增卡内文字必须同时删掉、迁走或机器化旧文字,不得把“以后再瘦”当交付;机器闸接管后删除卡内纪律副本并留指针。
3. **双上限由 `tests/run.sh` 阻断**:`laixin-pipeline` ≤220 行且 ≤12000 B;`laixin-plan-window` ≤180 行且 ≤20000 B。改上限属于治理规则变更,须创始人确认,⛔ 为让测试变绿顺手抬线。
4. 卡改动仍归各自 owner 验收;提交前跑全量测试。通信默认只发“动作 + 权威路径#锚 + commit”,交班正文落文件,⛔ 多条补发与 ACK-only。

## 判据方向

- **事实优先于措辞**:执行总表的措辞有无穷变体(`design-ready` 撞 `ready`、"已交付并在验收中"顺带提及、排队行滞后)。凡从台账取"什么可发/什么已合入",一律**交叉比对片名出现在哪一节**,不靠子串匹配措辞下结论(同族修复:`aebd38a` 油表、`9c5f82c` 下一片提示)。
- 工具自身不许重蹈它要解决的毛病:拿不到数据显 `?` 不显 `0`,"空比空"的相等不是相等。

## 检查器设计三约束(2026-08-19 立,来源=复盘页 #20⑤/#21/#22 三缺陷同根;新写或改任何检查器过一遍)

1. **状态感知**:判据不许建立在裸文本形态上——记录了历史的行同时包含新旧两个状态(备注格引被替换旧句/划线案底/更正段是知识库的**有意设计**),扫描器必须跳过备注格/删除线/引用块,否则与案底文化互相为敌(实撞:占位符 lint 对「刚消灭了占位符的那一行」报红);
2. **失效必须降级,⛔ 反向**:检查手段自身失效时,其表现不得恰好指向它要防的风险(实撞:verify-from 尾空行把人推回手抄 hash;裸 grep 判二进制让合规验收被判不合规)。零结果一律先探针自证再下结论,超时/读不了要自曝而非静默;
3. **噪声与目标行为同向**:检查器的误报不得随「被鼓励的行为」增长(实撞:audit-queue 误报与看板详实度成反比,长期会训练出更差的看板)——误报会随什么增长,设计时先答这一问。

**配套通用判据**:机器化边界=判据是否免语义(零 commit 可机器化,时间早晚/口径新旧不可);常驻循环体驻内存不随文件换代(改完提示 stop/start,loop_stale 已给);经软链调用 `stat` 必须 `-L`;`grep -c` 零命中退出码 1 要兜底;`ps lstart` 中文 locale 不可解析用 `etime` 换算。

## ⚠️ shell 会静默改写你的内容(一晚四发,三个角色各中一次)

反引号被求值(`laixin-lane log`)/ heredoc 吞长脚本 / **管道退出码被最后一段覆盖**(`run.sh | grep` 使红测试静默入库,`af957f4` 记账)/ **中文 locale 下 bash 把紧跟变量的多字节字符卷进变量名**(`$commit——` 击杀 set -u,`af957f4` 修)。⇒ **凡经过 shell 的文本与判据,验证"到达的内容/真实的退出码",不验证"发送的内容"**;变量后紧跟 CJK/全角标点一律 `${var}`;提交门控用命令自身退出码,不用管道尾段的。

**第五发(2026-08-14 02:1x,危害最大——它藏在测试判定本身)**:`set -o pipefail` 之下 `echo "$out" | grep -q`——**grep 命中即早退,echo 吃 SIGPIPE(141),pipefail 把"匹配成功"判成失败**;且与输出大小/机器负载相关(管道缓冲吸不吸收),表现为**"安静时全绿、流水线忙时 9 项无辜测试齐红"**(实测 20/20 复现)。⇒ 测试判定一律 `grep -q "$want" <<< "$out"`(herestring 无管道)或先落盘再 grep;**一套会因机器忙而变红的测试,比没有测试更糟——它教会所有人忽略红色。**

**第六发(2026-08-22 21:5x,11B 归口监测中实撞)**:**bash 3.2 下 `source <(sed -n '/^fn()/,/^}/p' 文件)` 函数不落地且不报错**——`bash -c 'source <(…); fn'` 回「command not found」,同一段文本落临时文件再 `source` 即正常。症状与「抽取范围写错」完全同形(都只看见函数不存在)。⇒ 测试夹具一律 **落临时文件再 source**(套件既有夹具全是这形态,照抄);另:测试名字符串里 ⛔ 带反引号(`t "… \`K3\` …"` 被当命令替换)。
