---
name: laixin-acceptance
description: 来信平台独立验收卡——Codex 交付一片后验收、合并前把关时使用。核心原则:不接受任何自我声明,只接受可复现的证据。防已发生过的失败:报告称做了实际没做、报告没提的验证项恰好是坏的、验收时误合并分支、合并后忘记更新台账(四处,含排队节整行移出)。Use when the user asks to 验收/复核/审查 a 来信 branch, decide whether it can merge, or write its acceptance record. 触发词:来信验收、验收记录、合并、Codex 交付、来信 main。
---

# 来信 · 独立验收卡

> 唯一原则:**不接受自我声明,只接受可复现的证据**。"跑过了""验证通过"不算证据,命令输出和实测轨迹才算。
> 只用于 `/Users/pingxia/来信平台`。交付报告是线索,不是证明。
> 全部红线以 `~/Obsidian/项目入口/来信平台/知识库/索引/wiki-红线清单.md` 为准,本卡只给程序。
> **证据锚定(2026-08-13 事故后立)**:结论里每个数字、每处代码引用,必须能在紧邻的真实工具返回中逐字找到;记不起对应返回的,重新执行工具,**不得凭印象书写**。编造证据比漏检更危险。
> **证据落盘(同日加固,把上一条从靠自觉变成可复查)**:凡结论要引用的输出,一律 `2>&1 | tee <证据目录>/<名字>.log` 落盘再引用——全量两次 `pytest-1.log`/`pytest-2.log`、迁移回环 `migration.log`、自写复现 `repro.log`、范围与红线 `scope.log`。证据目录由 `laixin-lane verify` 建好并写在派单里(`~/.laixin-evidence/<片名>/`)。**派工窗口会用 `laixin-lane evidence <片名>` 复查,回执里的数字 grep 不到就退回重验。**

## 先建立独立的审查面

动手测之前,先读:权威 prompt、它引用的设计、执行总表当前状态、候选分支本身。

- **从独立 worktree 或 ref 级操作审查**,绝不在开发方正在用的工作树里 checkout/merge;
- **要求 `main` 是候选分支的祖先**(`git merge-base --is-ancestor main <branch>`)。不是 ⇒ 退回 rebase,并**重跑受影响的检查**——旧基点上的自测与走查验的是旧页面,结论无效;
- 确认候选只改了它声明的范围:逐项看迁移、共享配置、相邻 UI/API 面有没有意外重叠。
- 🔴 **走查的控制端一律是 `browser-harness`,⛔ 去找 `mcp__node_repl__js` / 内置浏览器工具那一类**(2026-08-19 codex 验收窗两次实撞,第二次逐字报 `BROWSER_NODE_REPL_JS_AVAILABLE=0` 因而跳过走查;11B b7 记档)。**为什么你找不到那类工具**:验收窗起窗时 **MCP 是被有意全关的**(`lane_mcp_off_flags` 把 `node_repl` 等十项 `enabled=false`)⇒ **你报「本会话没有 X 运行时」说的是事实,但那不是环境坏,是控制端选错了**。✅ **正确路径**:`browser-harness` **已在验收窗的允许工具里**(`VERIFY_ALLOW` 含 `Bash(browser-harness*)`),shell heredoc 调用它是标准用法;它会读你环境里的 `BU_NAME`/`BU_CDP_URL`,⛔ 自挑端口 ⛔ 连共享 Chrome。⚠️ **⛔ 因为找不到内置浏览器工具就跳过走查并把它写成打回点** —— 那是把一个自己没做的验证项记成被验对象的缺陷;**走查做不了要停车问派工方,⛔ 写进回执当缺陷**。
- 🔴 **CDP 走查:`BU_CDP_URL` 在而端口无监听 = 你还没起,⛔ 判「环境坏」⛔ 要求派工方提供**(2026-08-19 同形第三例,前两例在开发方、**本例在验收方**;11B 创始人窗口 b7 记档)。**判别表**:①`echo $BU_CDP_URL` **有值** + `lsof -nP -iTCP:<端口> -sTCP:LISTEN` **无监听** ⇒ **你还没起自己的 headless**,照宪法头第 12 条的照抄块起(`PORT="${BU_CDP_URL##*:}"` → `--headless=new --remote-debugging-port=$PORT` → `curl /json/version` 自证);②`BU_CDP_URL` **真的为空** ⇒ 才是起窗器的问题,报出来;③端口**有监听但 `/json/version` 五秒无响应** ⇒ 僵死态,`lsof -t` 精确取 PID、`kill -9`、重建 profile 后重起。④**(2026-08-19 晚补,真实 codex 验收窗实撞)端口有监听、`/json/version` 也答得出,但连接被主动拒绝**(`Connection refused` / `unreachable after 30s`)⇒ **⛔ 判「变量为空」,也 ⛔ 判「僵死」** —— 这两条修法都不对症。**分清方式=换一个观察位置**:从**另一个窗口**跑同一条 `curl http://127.0.0.1:<端口>/json/version`。第三方连得上而你连不上 ⇒ 是**你这个窗口的网络可见性**(代理/沙箱)问题,与环境变量、与被验对象都无关;两侧都连不上 ⇒ 是 **CDP 起停时序**(端口 LISTEN ≠ CDP 就绪),等或按③重建。⚠️ 现场逐字样本:`browser-harness: fatal: BU_CDP_URL=http://127.0.0.1:9889 unreachable after 30s: Connection refused`,而同一时刻 `lsof` 显示 `TCP 127.0.0.1:9889 (LISTEN)`、`/json/version` 返回了完整的 `webSocketDebuggerUrl` —— **fatal 消息本身带着变量的完整值,所以它绝不是「读不到」**。⚠️ **⛔ 把「我连不上」写成「变量为空」** —— 那是**诊断结论不是观测**,而验收回执是合并依据:**开发方误判只是停车,验收方误判会把一个自己没做的验证项记成被验对象的缺陷**。📌 实例:某验收回执写 `BU_CDP_URL_PRESENT=no` 并据此跳过 CDP 走查、写成打回点要求「派工方提供专属通道」;派工方两次实测该窗环境**逐字有值**(`BU_CDP_URL=http://127.0.0.1:9889`)而该端口**无监听** ⇒ 打回点不成立。
- ⚠️ **已知环境差异:`frontend` 的 `npm run test:supply-collab-copy` 在「不位于 `~/` 下的审查树」里必 ENOENT**(2026-08-19 两名验收方各撞一次,11B 创始人窗口 b7 自核记档)。逐字根因:`frontend/scripts/check-supply-collab-copy.mjs:13` 用 `readFileSync(resolve(process.cwd(), "../../Obsidian/项目入口/来信平台/知识库/索引/wiki-供给侧词汇表.md"))` —— **路径按 `process.cwd()` 上溯两级**。⇒ 在 `~/来信平台/frontend` 或 `~/来信平台-b87/frontend` 下解到 `/Users/pingxia/Obsidian/...` **成立**;而审查树若建在 `/private/tmp/.../scratchpad/acc-cand` 之类**不在 `~/` 下**的位置,上溯两级落在 scratchpad 里 ⇒ **文件不存在**。⚠️ **⛔ 把它读成「worktree 下必失败」** —— B 轨开发方的 `~/来信平台-b*` worktree **不受影响**,误读会让开发方以为自己也会撞。**绕过**:把审查树建在 `~/` 下,或跑该测试时 `cd` 到主树 frontend,或临时 symlink;**⛔ 因它失败就判该片有问题** —— 它是环境差异不是代码缺陷,但**要在报告里如实写明你用了哪种绕过**。**根治**(来信侧 B 轨微片料,已登记):路径改用绝对路径或环境变量(与 `laixin-lane` 的 `LAIXIN_KB` 同哲学),⛔ 依赖 `process.cwd()` 的相对上溯。

## 验收七步

**1. 自己跑,不看报告结论**
全量测试**跑两遍**(第一遍验通过,第二遍验无随机污染);迁移片必跑 upgrade→downgrade→upgrade 回环。判据是"**全部 passed 且 zero failed**",不是测试数相等(`unittest.subTest` 计数不稳)。
> 失败样本:Codex 的测试是分三批跑的,合起来才全绿;验收方整跑两次才发现口子。

**2. 报告没提的验证项,就是最该亲手跑的**
把交付报告的"已验证清单"和"本片改动清单"对比,**差集就是你的重点**。至少亲手测一个报告完全没提到的实质风险。
> 失败样本:M-11 报告验证清单恰好漏列 downgrade 回环,而那里正好有真 bug(裸 drop_column 删带 FK 列)。

**3. 声明抽查**
报告说"写了 X",就去看 X 是否存在;说"改了 Y",就 `git diff` 看 Y。
> 失败样本:S3 收口报告称已写验收记录,实际没写。

**4. 界面必须真实走查**
browser-harness 实测,不看截图不看描述;消费者侧核 375px 无横滚;匿名能力必须**无痕环境**(清 localStorage/sessionStorage/cookies)实测;**深浅主题都要看**——`R48` 是唯一"后端测试与 lint 都抓不到、只有肉眼能抓"的红线(自定义底色区块未声明前景色→暗色主题下内容不可读),凡新增底色块的前端片逐块核。**页面能渲染不等于通过。**
> 失败样本:B2 的 Codex 扩展断开没做走查,验收方补做才发现问题;订单-4 的"无注册墙"只有无痕实测才能证;B5 四处浅底区块暗色下不可读(锁定版正文、实得金额、联系方式)。
> 走查报"超时"先怀疑环境:自测端口可能有挂死进程(见《开发环境与运行说明》)。
>
> **"建了单一映射源"≠"所有直出点都改用了它"**:凡验"内部状态→人话文案"类交付,必须**扫渲染后的页面文本并遍历状态全集**,不能只确认映射表存在。三元表达式形态的兜底(`x === "paid" ? "已确认" : x`)最容易漏,`grep "|| status"` 抓不到。
> 失败样本:b11 建了 consumer-copy.ts 且修好订单状态与事件,却漏了资金区——非 paid 状态直出 `pending_confirm`,验收方复走页面才捞出(2026-08-12)。

**5. 红线逐条核**
按本片相关的 R 编号逐条核(见红线清单);可见性问题对 `索引/wiki-主体视角表.md` 逐格核。**"不可见"必须是接口不返回,不是前端隐藏**——用**权限最低的身份**直接打接口验响应体,不看 UI 有没有藏。

**6. 边界核对**
`git diff main --stat`:B 轨必须零迁移、后端仅新增独立只读文件;A 轨确认没碰不该碰的共享文件。

**7. 收尾纪律(2026-08-13 五窗口拓扑后修订)**
- **最后一步必须是**:rebase 到当时 main,并**在 rebase 后的状态上复跑全量**——"验的就是合的";
- **你不合并**。合并权在派工窗口:回执交出 `verify-*` 分支名 · 其 commit · **所基于的 main commit** · 全量结果,由它校验 main 未变后合入;
- 主树被 A 轨 Codex 占用期间,**禁止在共享工作树 checkout/merge**;起后端服务只能从与 main 一致的树起。
> 失败样本:B1 验收时误将分支合入 Codex 正在用的 order-2 分支。

## 合并后必须更新四处(派工窗口合并后落实;验收窗口只负责验收记录)

1. `log.md` 记一笔(合入 commit、用例数、验收要点、过程偏差);
2. `4-开发层/来信平台-执行总表.md` 更片级状态;
3. `索引/wiki-模块台账.md` 更"已开发/下一步";⭐ **同轮核该行的「所属版块」列**(2026-08-21 局七 R4 拍定、首批落地落列;**版块=推进线**,本表 26 行降维为块内主题线程账)——**该行该列必须有值**;若值为 `⚠️需裁` / `⚠️需实查`(现有 6 行),**⛔ 当成已归好的域用、⛔ 自己顺手裁**,当轮报方案窗口。🔴 **块枚举在机器生成映射落地前 ⛔ 标 `active`**(合流案「守门先于铺开」),故本列现为**实读初值**不是已生效枚举;**若本片改变了运营可用面**,再更《运营启动计划》"工程现状"表(只写实测通过的);**若本片产生了可复用机制**,登记 `索引/wiki-机制复用表.md`(登记前知会另一窗口,防重复录入);⭐ **共用组件的新增、或其配色/交互形态的确立,默认视为可复用机制必登记**(2026-08-14 实撞:同日两片对「浅底组件配色」各自正确地发明两种解法——散装不是历史遗留,是当天做出来的;第二片选解法时必须能查到第一片选了什么);
4. **把该片在执行总表「排队」节(及任何"可自写/候选"清单)的行整行移出**(2026-08-14 增,方案窗口第四任裁)——⛔ **不用标记代替移出**:删除线不是移出、机器分桶只认状态格首段,标记类方案已两次被证明对机器不可靠;与「进行中节整行搬走」同一条机械纪律。**排队节是靠人记得回去划掉的那一步,恰恰是合并时注意力不在的那一步——所以它必须在清单里,不靠记性。**

> ⚖️ **合规/可见性收敛类片的第三处替换规则(2026-08-14,方案窗口第五任确认 dispatch 判断)**:这类片不推进功能模块 ⇒ **第 3 处不落模块台账,改落其事实所在的权威索引表**(主体视角表或红线清单的对应行,状态就地更新)。四处=log+执行总表+视角表/红线清单+排队节移出。首例:R4 收敛 `c9b42e1`。
>
> ⭐ **销账必须写回被引用处**(2026-08-14,同一行陈旧登记连绊两窗后立):执行总表销了账、模块台账/策划正文还写着"待办",读到的每一方都会再被绊一次——**销账动作以"它被引用的那些地方"为完成边界,不以第一张表为边界**。

不写入任何密钥或测试数据。

## 问题分级

- 验收方可修的小口子:直接修 + 在验收记录里写明谁修的;
- 方向性问题 / 越界 / 违反红线:**打回**,给出**最小可复现的失败**,不代为改设计;
- 过程偏差(未开分支、未提交、测试分批跑等)一律记录在案,即使无实害——偏差不记录就会重复。

## 自动机制类交付的两条必备判据(2026-08-20 C 族成规落卡;实证=#105 材料广播 fixture 560 全绿而真环境首发静默失败)

1. **真环境首火观测**:凡交付「应该会自动发生某事」的机制(广播/搬运/告警/自愈),验收判据必含一次**真环境首火**——指定观测人、观测点、「该发生而没发生就报修」义务;⛔ 以 fixture 全绿 + doctor 零错收尾。
2. **病灶级绊线**:修静默缺陷时,绊线必须**修成能复现该病灶的形态**(#105 例:原 fixture 用明文路径所以全绿,修复轮补真 git 中文路径仓)——首火管「上线时有人看」,绊线管「以后机器自己看」,缺一半回到静默。

## 本卡的失败样本台账(准入依据)

| 规则 | 失败样本 | 日期 |
|---|---|---|
| 自己整跑两遍 | 测试分三批跑合起来才绿 | 2026-08-12 |
| 报告差集重点查 | M-11 漏列 downgrade,那里有真 bug | 2026-08-11 |
| 声明抽查 | S3 称写了验收记录实际没写 | 2026-08-11 |
| 真实走查 | B2 未做走查;订单-4 需无痕实测 | 2026-08-12 |
| 映射源≠全覆盖 | b11 建了 consumer-copy.ts 却漏资金区,页面直出 pending_confirm | 2026-08-12 |
| 合并纪律 | B1 误合入 Codex 在用分支 | 2026-08-12 |
| 证据锚定 | 执行窗口夜间伪造测试数字、grep 结果与两次不存在的合并 | 2026-08-13 |
| 无取值即未验 | 片三-B 验证节取值零贴(1.7k 行噪声)/ 双形态-2 五项结论语气实未走查——均靠验收方重跑才知真假 | 2026-08-14 |
| 四处台账(排队节移出) | 排队节三行已合入片仍标「待写/待发」(打标留痕 `c0dd11c`/部署配置/只读露出 `725afdb`),派工窗口一轮选片连撞三次无谓调查,方案窗口也引其一荐了已合入的"燃料"——台账说谎则读它的每一方都把谎当事实 | 2026-08-14 |

> 准入规则:**没有失败样本,不准往本卡加规则**。

## ⚠️ 受控片检查项:diff 超出受控清单字面的构造,逐条溯源(2026-08-27 立,11C 主持分线第二任裁;dispatch 第七十五任落卡。实证=`钓不钓:B1后端基座与四实体`——验收通过、文件面判据全过,而「受控清单的粒度比文件面更细」这一层无人接)

**适用**:凡 prompt 或其上游 D 档带**受控更新清单**的片(形态=「本片将更新的 X 字段清单」+「验收判据=diff 恰为清单项」)。

🔴 **文件面判据过了 ≠ 受控清单过了**。「7 个文件全在允许面 / golden diff 空 / 禁止路径扫描 RC=1」只证明**没动不该动的文件**,**不证明动的那些文件里只做了清单上的事**。受控清单的粒度**细于文件面一层**,必须单独判。

**判法(机械,⛔ 感觉)**:
1. 把 diff 里**超出清单字面**的每一处构造单独列出(样本:清单只写「加两个可空键」,而实现还加了 `__post_init__` 校验);
2. **逐条溯源**:每一处必须能**逐字指到本档或其已钉上游档的某一句**;
3. **溯源得到 ⇒ 视为清单内**(判据=「实现本档已钉条款所必需的构造期校验」);**任一条溯源不到 ⇒ 超清单,打回**。

⭐ **为什么用溯源而不是用「合理不合理」**:「实现合同」与「顺手多做」**在 diff 上不容易分**,两者都像是「为了让它正确工作」。溯源把这个判断**换成一个可核的动作** —— 指得出原句就是前者,指不出就是后者。
⚠️ **先例效应是这条真正要防的东西**:放行一次「它实现的是合同里的状态机」,下一片就会有别的东西以同样理由进来;**溯源要求让每一次放行都留下可复查的指针,而不是一次判断被后人当成通例。**

**样例(本条的来源案)**:Trip 三条构造期校验逐一溯源 —— ①`ended_by`/`ended_at` 成对=D6 §二「终止元数据」两键同生同灭 ②`ended_at` 带时区=D1 总则「时间 ISO8601 带时区」 ③`ended_by` 空而 `end` 非空拒绝=D6 §二「end 只在用户报告时写入」+「⛔ 已终止→active」的结构化。**三条全溯到,零新语义 ⇒ 清单内放行**。

## ⚠️ commit 数判据的整改轮口径(2026-08-25 立,方案窗口第二十九任裁「路 A」;实证=`快报办结需求公开端点` 第 2 轮打回——**同族第二次咬人**,首次=R1 第 3 次打回 2026-08-20)

**权威原文在 `laixin-kickoff` 卡「「每任务一 commit」的整改轮口径」节,本节是指针不是副本,冲突以那里为准。**

**口径**:交付 commit 数 = **首轮 1 加 每个整改轮 1**;交付方自证行写「commit 数 N = 首轮 1 加 整改 N 减 1,每 commit 对应一个任务单」。
⇒ **⛔ 因「分支相对 main 的 commit 数大于 1」打回一个整改轮候选**,除非**某个 commit 对应不上任何任务单**——那才是真越界。
⇒ 反向也成立:**⛔ 因「整改轮把两个任务塞进一个 commit」放行**,可分辨性是这条判据的立意。

🔴 **为什么这条必须写在验收方的卡里(本节的立项理由,⛔ 删)**:2026-08-20 立此口径时**只改了 `laixin-kickoff` 卡**——那是**开发方与写单方**读的卡;**验收方读的是本卡与交付报告,从来读不到它**。
⇒ 2026-08-25 快报片第 2 轮,验收方按该片 prompt 逐字的「交付 commit 数必须恰为 1」判打回,**它引证准确(`prompt:71` 真实存在)、复现命令可跑、分类正确、正文与末行一致——在它的射程内每一步都对**。
**这不是验收方失职,是材料没送到它手上**(「引用规则去判某方违规之前,先核那一方能不能读到它」的镜像形态:那条讲**判违规时**要核可达性,本条讲**改口径时**要送达)。

⭐ **通用教训(本节最值钱的一句)**:**一条判据的口径改了,要扫它的全部载体** —— 开发方 prompt 模板 / 写单方与开发方的 `laixin-kickoff` 卡 / 验收方的本卡,**三处缺一,缺口就会从没修的那一处原样再犯一次**。而**没修的那一处与修好的那一处,在「口径已经改了」这个认知里长得一模一样**。

## ⚠️ 边界四条实跑——全片通用判据(2026-08-20 立,R1 J3 改判配套;方案窗口第十六任裁,dispatch 43 拟稿)

**自「R1 模块 README 与边界契约起步」合入 main 起**,每片验收的标准判据里加跑四条(命令名已对 R1 分支实物逐字核过):`npm run test:imports`(import-linter,后端契约)· `npm run test:dependencies`(dependency-cruiser)· `npm run test:circular`(madge)· `npm run test:reduced-motion`——**各贴完整输出,⛔ 只写「已跑」**。前三条有聚合入口 `npm run test:boundaries`,分跑聚跑均可,但每条输出都要可见;`npm run lint` 链已含全部四条(R1 整改轮 `fb12d15`),跑 lint 贴全输出亦等价。

- **为什么落在验收卡而不是 workflow 文件**:本项目**当前无 PR 合并流程**,合并是 dispatch 的 ref 级快进 ⇒ 即使建 GitHub Actions,红灯也拦不住任何合并——那是标准的 R42 假门(能力上线、门没装在能拦住的位置)。本项目的真执行点是**流水线验收**,「违反即红」的落点必须是验收判据。
- **射程:全片通用,⛔ 只在动前端的片上跑**——import-linter 与 madge 管的是后端与全仓依赖边界;下一节「前端片必跑全部 `test:*`」照旧,本节是它面向全片的扩展,两节并行不替代。
- **GitHub Actions 冻结候审**:项目转 PR 合并流程后再立,届时本条与 workflow 并存(验收判据管「合并前」,workflow 管「推送后」)。
- **R1 合入前的过渡**:R1 尚未合入时,四条命令在 main 上不存在,本节不生效 ⛔ 拿它拦 R1 之前的片;R1 本片的验收按其 prompt 改判后判据走。**生效前提补(2026-08-20 15:11)**:本节生效还以「四条在**不自带 venv 的验收树**可实跑」为前提——R1 第 4 轮打回项(`test:imports` 写死 `cd .. && .venv/bin/lint-imports`,无 venv 树 exit 127)的整改判据①即此;该整改未合入前本节推迟生效。

## ⚠️ 前端片必跑全部 `test:*` 脚本(2026-08-13 新增,验收方自曝的缺口;同日改为不写死数量)

**只跑 pytest + lint + build,看不见前端单测** —— 它们既不在 lint 里也不在 build 里。

**样本**:`供给侧存量映射表收拢` 的验收方第一轮只跑了 pytest 与 lint/build,**漏了四个前端测试**;第二轮顺着"文件零重叠 ≠ 零耦合"这条线查依赖方时才发现 `frontend/src/lib/status-label.test.mjs:3` **直接 `import ./supply-copy.ts`** —— 正是那片改的文件。补跑后四个全绿,但**第一轮的"通过"当时并没有覆盖它们**。

⇒ **验前端片时,当场 `grep '"test:' frontend/package.json` 列出全部 `test:*` 脚本,逐个跑并贴退出码。判据是「全部都跑了」,⛔ 不是「跑满 N 个」** —— 写死的清单会随上游片过期(实撞:`双形态-1b` 加了第 6 个 `test:advisor-upgrade`,而当时四份 prompt 都写死"五个测试";与 kickoff 第 11 条"下游核上游实际交付"是同一条的另一面)。

⚠️ **更一般的做法**:改了 `frontend/src/lib/` 下任何模块,先 `git grep -n "from .*<被改文件名>" -- frontend/src` **查出全部依赖方**,再决定要跑哪些测试。**"文件零重叠"只保证不冲突,不保证不耦合。**

📌 **配套的一条查证手法(同一验收方给的,值得推广)**:要证明"某次 rebase 前后某个子树逐字节未变",**别用推断,用 git 对象 hash**:

```
git rev-parse <commitA>:frontend    # 与
git rev-parse <commitB>:frontend    # 相等 ⇒ 该子树逐字节相同
```

⇒ 相等即可**原样继承**该子树相关的全部判据(逐字校验 / 走查 / lint / build),**重跑零信息增量**。⚠️ 注意 zsh 会把 `$VAR:frontend` 当历史修饰符,要写成 `${VAR}:frontend` 或加引号;并注意 `[ "" = "" ]` 会把两个空值判成"相等"——**取 hash 后先断言非空**,否则是假阳性。


## ⚠️ 浏览器走查脚本的色彩假信号(2026-08-13 新增,一晚两发同族)

1. ⛔ **`bg-gradient-*` / `background-image` 元素上,`backgroundColor` 取到的是祖先的颜色**——渐变只设 `background-image`,`background-color` 保持 `transparent`,脚本回溯祖先就拿到父级白底。实撞:琥珀渐变底白字的 L4 徽章被报成"白底白字 1.00",险些立片去修一个不存在的缺陷。**凡待测元素自身带 background-image,对比度结论一律无效**——跳过并如实标注,或改用像素级采样;
2. **`lab()` / `oklch()` 等新色彩函数,老的 rgb 正则解析不了**(同晚第一发,R48 验收方自修过 rgb 层,渐变层没覆盖到——修好一层不等于修好一族);
3. ⭐ **通则:脚本报出的"极端值"(对比度 1.00 这种)优先怀疑解析器,再信结论**——真正的极端缺陷(主视觉白字白底)通常早被人眼发现;**"脚本发现了人眼从没发现过的显眼问题"这个组合本身就是先自查的信号**。

## ⚠️ 观察项不许挂在裁定分支下(2026-08-14 凌晨,1b 验收方自我认领)

**一个问题在两种候选裁定下都成立时,⛔ 不许写成"若按 (b) 裁定则…"这种条件观察项** —— 实撞:1b 第 1 轮验收把「区3 缺等级过滤」挂在「若 P1 按 (b) 裁」的条件下,裁定选了 (a),问题以另一形态存续、整改后反而成了区3 的唯一内容,拖到第 2 轮才打回。⇒ **写观察项前先问:换一种裁定,这条还成立吗?成立就无条件记。**

## ⚠️ 消费者可见片必带「文案口径」节(2026-08-15 新增;换人页 prompt 三连漏触发)

凡交付含消费者可见文案的片,交付报告须**单列「文案口径」一节:逐句标明出自消费者词汇表哪一行,或标"自拟"**;验收方按词表逐句比对,自拟句逐句判(内部语零上屏/异常三问齐)。

样本:换人页 prompt 未查词表(kickoff 7),三连漏——按钮词用了词表已废的旧话术、漏了词表写死的"资金句必带"(消费者在"服务者要被换掉"那一刻最想知道钱怎么办)、漏了内部语禁上屏边界。⭐ 缺口被抓到的方式值得记:**是词表登记的台账收尾动作把人按到了该读没读的索引表前**——台账收尾兼任漏网缺口的捕捞网,跳过登记=拆掉这张网。

## ⭐ 反向假门(R42)类缺陷的取证范本(2026-08-20 立,方案窗口第十六任落;样本=招募频道前端方向性打回,c8 验收方首创)

怀疑「后端在、前端门没了」时,标准取证两步:
1. **main 与候选同构对照**:同一后端、两侧各起 build 产物,同一身份同一路径各走一遍,取「入口存在=True/False」的成对读数——单侧截图证明不了「是本片删的」;
2. **唯一入口漏斗追踪**:grep 该动作的 API 调用点证明全前端仅此一处,再从注册/引流起点逐跳追到该入口——证明删的不是冗余入口而是**主漏斗的必经门**。
两步合起来把「方向性打回」从观感判断变成可复现事实;该案报告 §五 披露质量高是一眼定位的前提(披露纪律的正反馈实证)。

## ⚠️ 回执引用数字必须可一跳复查(2026-08-14 新增,方案窗口裁;生产引导片触发)

> 样本:生产引导片回执三个数字 `admins=1`/`boot_events=1`/`total=31` 逐字 grep 证据文件**全部零命中**——证据里它们是表格形态(表头一行、数值另一行)与 `total= 31`(等号后带空格)。数字全部属实,验收方没做错任何事:**善意的紧凑转写提升了回执可读性,同时关掉了为它设计的 evidence 复查机制**(2026-08-13 编造事件后立的那道防线,被排版绕过)。

- **规矩**:回执引用证据数字,**要么保持证据文件里的原始形态,要么给「文件名:行号」指针**——**优先后者**(复查方一跳即达,连 grep 都不用);可读性转写(key=value 等)允许并存,但必须挂在指针或原始形态旁,⛔ 不得只给转写;
- **复查方配套**(派工窗口既有做法转正,防漂移):**零命中 ≠ 编造**——先换探针核形态差异(表格/空格/换行),查明再判;直接按编造退回会冤枉一份真实回执;
- 📌 **范本句**:同片验收方对零命中判据"先用同一把尺在跑过 seed 的 dev 库自证会命中,再扫引导后的生产库"——kickoff 12 第 3 条「先证明探针本身有效」的**首次主动做满**,以此为准绳。

## ⚠️ 无取值即未验(2026-08-14 晨新增,同日两片各撞一面)

> **验收时逐项对照 prompt 验证节的编号。凡该编号对应的取值/命令输出在报告里找不到,一律按「未验」处理并亲手跑** —— ⛔ 不因为报告写了「通过/正确/符合」就跳过;**报告的结论语气不构成证据。**

**为什么判定单位是 prompt 的验证项编号而不是报告的段落**:报告结构是自由的(取值可以写「见上」、可以淹在 1.7k 行日志里),「同条目无取值」不可判定;prompt 的编号是双方共有的清单,验收方本来就要逐项过 —— **举证责任在报告:它要证明自己验过,不是验收方证明它没验。**

**两个样本(同日,对验收方的伤害相同——都得重跑一遍才知真假)**:
- `片三-B`:工作做到了,**报告淹没证据** —— 1.7k 行 seed 噪声,验证节取值一个没贴;
- `双形态-2`:**报告语气强过证据** —— 走查脚本未落盘且未访问升级页,五项判据用结论语气而实际未走查。

📌 配套开发侧软规则在派工窗口 prompt 模板(脚本/走查类验证,脚本本身落盘进证据目录并写明路径 —— **取值可以手写,脚本能被重跑**)。
⚠️ **已知未覆盖,如实登记**:「脚本存在」≠「脚本覆盖了这条判据」(双形态-2 的脚本即使落了盘也没访问升级页)—— 修法需每条判据标注证据来源(`[单元测试]`/`[脚本:路径:行]`/`[人工]`/`[未验]`),但核脚本内容的成本可能超过收益,**待第二个样本再议**(准入规则)。

## ⚠️ 重建/替换类片默认判据:旧物残留扫描(2026-08-15 新增,方案窗口第六任裁;venv 重建片验收方首创)

凡候选片**重建或替换某个环境/载体/文件**(venv 重建/表重建/文件迁移/配置替换),判据默认包含**旧物残留扫描——只证明新物存在不够,要证明旧物真的不在了**。样本(venv 重建片,报告与第一轮验收都没查、第二轮验收方补上):`.venv/lib` 只有 python3.12 一个目录、23 个 shebang 指向 3.9 的为 0、`CPYTHON39_PYC_COUNT=0` ⇒ 证明是**干净的删除重建而非原地叠加**。原地叠加的残留(旧 shebang/旧 pyc/旧列/旧配置段)与新物并存时,「新物在」的全部断言照样全绿——这是重建类片的经典盲区。

## ⚠️ 资金族片默认判据:失败后可重试(2026-08-14 新增,方案窗口第六任裁;资金3-1 第 2 次打回触发)

凡候选片的写入路径碰**资金族载体**(账本 `ledger_entries` / `PaymentCallback` / 托管账户与 charge / 分账与出账),验收判据**默认包含「失败后可重试」——不论 prompt 写没写**(prompt 侧对应条=kickoff 卡「验证项怎么写」1-bis,管写 prompt 的人;本条管验收的人,双保险):

1. **失败一次 → 排除失败原因 → 再走同一条路,必须能成功**。判据止步于「失败时返回正确错误码」无效——**伤害发生在错误响应之后**(幂等键/锁/去重键/状态残留把下一次挡死,重试看似成功而钱不入账);
2. 路径上有**幂等键/锁/去重键/状态机跃迁**时,必问:「这次失败有没有在库里留下一个会挡住下次的东西?」;
3. **与基线树对比是最省的判法**:同一脚本在 main 与候选分支各跑一遍对比结果,⛔ 只在分支上自证不算;
4. **「无残留」探针先自证有效**(kickoff 12-3 同族)——两种无效形态已实撞:探针在调用前先改了状态字段因而命中另一条事务情形;用 mock 把被测函数整个换掉、根本没执行到插入那一步。

样本:`资金3-1 适配层先行` 第 2 次打回(2026-08-14)——确认收款失败后幂等键被 `PaymentCallback` 残留行永久占用,重试 HTTP 200 而账户仍 `pending_confirm`、钱不入账,且同族覆盖凡回调插入后抛异常的路径;**329 用例全绿没抓住**:新增错误路径用例全部止步于断言 409。

## ⚠️ 检查类交付的判据三问(2026-08-20 立,方案窗口第十六任落;来源=R1 第 5 轮逃逸,验收方三分法)

凡交付物本身是**检查/守护/校验类**(linter 接线、契约检查、对比度探针、守护脚本),判据必须三问齐,缺一该缺陷就能溜过:
1. **A·能跑**:目标环境里 RC=0(R1 案=无 venv 的 worktree 里 lint 全绿);
2. **B·没被关掉**:破坏其依赖时 fail-closed 报错(⛔「找不到就跳过」);
3. **C·检查的是对的对象**:**在目标环境里造一个已知违例,检查必须变红,还原复绿**——A 与 B 都区分不了「检查了错的对象」(R1 案:`test:imports` 切到主仓根跑,worktree 里恒绿假门,A/B 双双照过;同一候选因主树切分支 15:3x 绿 15:5x 红)。
⭐ 连带教训(载体改动复核):改判据载体时,核的不是「保住了哪些已知属性」,而是「**已知失败模式的触发条件在新载体上是否仍被覆盖**」——R1 案中派工方改载体、方案窗口复核,两道都过了,因为两边核的是同一个已知属性(无 venv),而新失败模式的触发条件(worktree 本身)当时不在任何人的清单上;三问 C 的存在就是为了让「不在清单上的失败模式」也有兜底探针。

## ⚠️ 判据设计四形态速查(2026-08-18 收编,创始人收班优化轮病灶三①;此前活在 dispatch 台账与交接包,跨任会衰减——卡是跨任跨轨载体)

设计或补写任何判据时,四形态逐个过一遍「用得上吗」:

1. **同构对照**:full 与 none(或 main 与候选分支)两条路各跑**同一份脚本**看 delta,⛔ 只在一条路上看绝对值(资金族默认判据第 3 条同源;桥片验收方靠它抓到 V5-bis 全绿假象);
2. **探针翻转**:下任何「零命中/无残留/没有 X」结论前,把**同一把探针指向已知存在的对照物**——命中即探针有效,零命中才可信;「我查过了」≠「我用对探针查过了」(kickoff 12-3 与本卡资金族第 4 条同源)。⚖️ **升级为核查前置(2026-08-19,dispatch 三十任一班四种探针失效自析后)**:**核查类探针使用前先做一次自证(全集数目对照/已知阳性命中),⛔ 一次性现写直接用结果**——四种已实撞失效形态:命中同名错对象(包含匹配)/同一 bug 历史使用未排查/排除式过滤误杀真命中(grep -v)/读取范围写窄漏目标;共同根不是「不够小心」,是**现写探针没有复核步骤**;
3. **成对判据**:被挡物与被保物**成对断言**(挡的 assertNotIn + 保的 assertIn),防「为挡 A 顺手挡 B」;收敛/收缩类片默认要求(kickoff 10-bis 有范本与固化要求);
4. **消费侧反查**:列出「还有谁在读这个状态/字段来算指标」的消费者清单(统计口径/榜单/结算/看板),**每个消费点至少一条判据**,⛔ 只沿「我改的函数做了什么」生产侧枚举(kickoff 第 10 条第四种长相;prompt 射程侧对应 kickoff 13-bis)。

**⭐ 取值来源通则(2026-08-18 补,R3 整改轮触发)**:**合格的数字可以来自不合格的地方——判据要连取值来源一起写死**。「贴改前取值」不够(实撞:取值是真的,只是借用了上一轮验收方的);写成「贴出**你自跑**改前对照的命令与取值,并贴出改后**同一探针**的取值,两者来自**同一次会话**」;mock 的取值来源同理是判据的一部分。

## ⚠️ 已知既有债口径速查(2026-08-18 立;一次性验收窗口无跨片记忆,同一既有债已被三个验收方各花一轮独立排查)

1. **建库/迁移回环探针撞 `NoSuchTableError: orders` @ `20260601_0002`** ⇒ **直接引口径判「既有债,非本片引入」,⛔ 逐片重新排查**;唯一例外=该片自己改了 `alembic/versions/` 下 `0002` 及更早的文件(`git diff --name-only` 判)。权威落点=[[来信平台-开发环境与运行说明]]「建库路径修复」条(2026-08-14 方案窗口第四任裁,内容锚定位 ⛔ 行号):`scripts/init_db.py`(create_all + stamp head)是唯一受支持的建库/恢复路径,「修完整迁移历史」已否决;从零部署走 `deploy.sh` 不撞该断点。
2. **通则(dispatch 二十六任实撞后立)**:转述或引用台账里的「待办/未解决」条目时,**「它还在台账里」≠「它还没被处理」**——销账动作可能发生在另一份文档里;判「无人处理/设计没写」之前,按 kickoff 12-bis 先全知识库关键词 grep 一遍。

## ⚠️ C 轨片叠加层(2026-08-20 01:1x 立;创始人指出「C 轨成常设后没设验收」,11B 归口 pingxia-37 核事实、方案窗口第十四任裁授权-2;**权威正文=协作流程 C 轨节 §七「C 轨片的验收」,本节是镜像,两处冲突以协作流程为准**)

> **射程**:第 1/2/4/5/6 条=分支/worktree 以 `c` 开头、或派单写明「C 轨」的片;**第 3 条(验收引擎)=任何含浏览器走查判据的片,⛔ 限轨**。**七步照跑不减**,本节只叠加。机器路径不分轨(事件总线按末行标记扫交付、`verify-from` 只解析末行),⇒ 缺的不是能不能验,是**验什么**。

1. **生产产物层(C 轨片必加,AB 轨不变)**:C 轨是唯一测生产产物的轨,dev 走查 + 全量**结构上测不到编进产物的缺陷**(`NEXT_PUBLIC_API_URL` 编进产物躲过 5 天例)⇒ 浏览器走查对象=**`next build` 产物经 `next start` 服务**(⛔ dev server),`NEXT_PUBLIC_API_URL` 按生产形态设为**非空且非 loopback**、指向验收后端;验收记录写明产物 hash/目录与 env 取值。**判据逐字**:「走查截图所对应的 URL 由 `next start` 进程服务(`ps` 证明),dev server 零在跑」。⚠️ 与本卡「采集前断言当前标签页 URL」纪律叠加执行:先断言 URL,再证明该端口是 `next start` 在服务。
2. **反馈清单即验据**:真人实测 / 全站检测回流的修复片,prompt 必带「反馈清单」节(逐条=来源 · 现象 · 复现步骤 · 期望态),交付报告逐条回填「复现 → 修法 → 复走结果」;**验收窗按清单逐条复现再复走**,漏一条=打回(工程)。prompt 无该节或报告未逐条回填 ⇒ 先核这是 dispatch 模板缺口还是开发方漏项(前者报 dispatch 补,后者打回)。
3. **验收引擎临时钉 claude——射程=任何需要浏览器走查的片,⛔ 限轨**(协作流程 §七 第 3 条 2026-08-20 01:52 扩射程;镜像):凡 prompt 含浏览器走查判据的片(C 轨片天然全是;A/B 轨带 UI 控制位/页面改动的片同样),dispatch 起验收窗一律 `LAIXIN_VERIFY_ENGINE=claude laixin-lane verify-from …` 单次覆盖(零改码);纯后端片照旧 codex。**原因=两个半边都要**:通道要由 dispatch 预起 **且** 验收窗自己读得到端口——codex 验收窗实撞「`ps eww` 读到 `BU_*` ≠ 它的 Bash 读得到」(b87 / A 轨片⑥ 双样本,片⑥白跑 17 分钟后正确停车)。**解除条件**=11B 报 `BU_*` 悬案判死 **且** codex 验收窗在真环境完成一次 ≥16 组真实走查;解除当轮回写协作流程 §七 与本节。**验收方自查**:起窗后第一件事在 Bash 里 `printf "%s %s\n" "$BU_NAME" "$BU_CDP_URL"`,两者任一为空 ⇒ 按宪法逃生口停车报告写明「变量为空」(⛔ 写成「连不上」),⛔ 自选端口或借共享 Chrome。
4. **两态台账**:UI 重设计片「验收通过」≠ 动效终审过——`review-animations` 终审须创始人/人类显式运行 ⇒ 回执与台账状态写 **「验收通过 · 待创始人终审」**;可合 main,**部署载荷含该片时创始人终审(或真人实测)是部署前闸门之一**;⛔ 验收方自称已过终审。
5. **并发**:原「C 轨先跑单片、⛔ 与 AB 轨同时进验收」已解除(临时专项期口径);常设后按资源限:同时在跑的验收窗 ≤ 三轨各一、CDP 通道由 dispatch 预起且各窗端口唯一。
6. **对照份命名(11B 机器侧)**:引擎后缀交付报告 `X-交付报告-kimi.md` 片名派生为 `X-kimi`(⛔ 与对照份同名——回执 `X-验收回执.md` 会互相覆盖);回执落 `X-kimi-验收回执.md`。

## ⚠️ 一个证据文件只允许一个写入方(2026-08-18 立,验收方自报偏差)

两个进程各自 append 同一日志文件(后端全量后台跑 + 前端结果 `tee -a` 同一文件),后者的文件句柄不感知前者的写入 ⇒ **日志看起来完整、实则被覆盖截断,且不报错**——丢的是前端第一轮取值。与「检查手段自身失效」族(evidence 显示上限/grep 二进制缺失/PIPESTATUS 大小写)同源,**区别:坏的不是判据,是承载取值的容器**。规矩:每个证据流一个独立文件,合并展示用只读拼接;发现容器损坏后**重跑补齐 ⛔ 凭印象补写**,并把损坏记进看板(样本验收方两条都做对了)。
