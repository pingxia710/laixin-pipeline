---
name: laixin-pipeline
description: laixin开发流水线——五窗口拓扑下派工窗口与验收窗口的操作卡:用 laixin-lane 向 tmux 里的双轨 Codex 派工、监听交付报告落盘、交接给验收窗口、独立验收后回执合并;含交接协议、上下文换班纪律、看板记录、夜间无人值守规则。触发词:流水线、派工、验收窗口、laixin-lane、监听交付、指挥Codex、发车、接管流水线、换班。
---

# laixin开发流水线(编排卡)

> 权威流程在 [[来信平台-开发协作流程]]"laixin开发流水线"节,本卡只给操作程序。
> 铁律一:**自动化只搬消息,不动闸门**——发车位纪律、验收七步(laixin-acceptance)、基点纪律原样执行。
> 铁律二:**状态永远在知识库,不在窗口记忆里**——执行总表(片级)+看板(事件流)随做随写,任何窗口随时可被换掉。
> 铁律三(2026-08-13 事故后立):**验收结论里的每个数字、每处代码引用,必须能在紧邻的真实工具返回中逐字找到**;记不起对应返回的,重新执行工具,不得凭印象书写。

## 0. 先确认你是哪个窗口

2026-08-13 起,原"执行窗口"按职能拆为两个:

| 你是 | 你做 | 你**不**做 |
|---|---|---|
| **派工窗口** | 写 prompt、`laixin-lane` 派工、监听交付与轨道状态、交接给验收窗口、收回执后合并、更新执行总表 | **不验收**(不许自己验自己派的片) |
| **验收窗口** | 只做独立验收:复现证据、跑全量、browser-harness 走查、rebase 后复跑、写验收记录、给结论 | **不写 prompt、不碰 lane、不合并** |

不确定时问创始人或方案窗口,**不要两个都干**——那就是拆窗口前的旧形态。

---

# A. 派工窗口

## 工具速查(`laixin-lane`,已在 PATH)

```bash
laixin-lane fresh a                        # 重启 A 轨=全新上下文(默认 ~/来信平台 主树)
laixin-lane fresh b --dir ~/来信平台-bN    # B 轨每片换 worktree(必须独立 worktree,不可用主树)
laixin-lane fresh a --with-mcp aliyun-readonly  # 按片放行 MCP(默认全关只留 codegraph;部署/运维片需要阿里云通道时带此参——创始人 2026-08-18:默认关、选择性开)
laixin-lane send a "好的,接下来开发 /Users/pingxia/Obsidian/项目入口/来信平台/知识库/4-开发层/prompt/<片名>开发prompt.md"
laixin-lane peek a 30                      # 抽查在飞状态;一律 ≥20 行,5 行只截到提示符会误判
LAIXIN_WINDOW=派工窗口 laixin-lane log "合并 客服-1 <commit> 195绿"   # ← 派工窗口记看板用 log
                                           #   vlog 是验收窗口专用(来源固定标"验收窗口"),你用了会记错来源
laixin-lane status / attach / down <a|b>

laixin-lane verify-from "<报告>" --prompt P --to dispatch   # 一步式进验收(解析末行,防手抄 hash 错)
laixin-lane next-migration / next-worktree # 发车前占号校准(未合并分支/未提交文件都占号)
laixin-lane events status                  # 事件总线(dispatch 自动带起;交付+卡住事件投递)
laixin-lane handoff                        # 交班:客观半部机器生成,判断人写
laixin-lane claim                          # 接管派工角色的第一件事:认领派工权(send 会自动续期)
# 模型已显式钉死(2026-08-13 创始人裁定):验收/派工窗口 = claude-opus-5(agentic 主力档,验收要的是可靠核查
# 不是极限推理);方案窗口 = 创始人自选(Fable 5);lane Codex = gpt-5.6-terra。**不继承任何人的 /model 副作用**
# ——有人在方案窗口切模型存为默认,流水线成本不该因此翻倍。改档:LAIXIN_VERIFY_MODEL / LAIXIN_DISPATCH_MODEL。
laixin-lane whoholds                       # 谁当前持有派工权
laixin-lane doctor                         # 接班第二件事:体检 skill落位/命令/知识库/拓扑/发车位/工程债
laixin-lane ctx                            # 上下文占比(≥70% 是交班硬闸门;自我感知会低估 15-25 个点)
laixin-lane stats                          # 流水线节奏(静默占比)+ 队列燃料(还能跑几片)
laixin-lane evidence <片名>                # 合并前复查验收证据(回执数字须在此 grep 到)
laixin-lane watchdog start|stop|status     # 看门狗(需先 laixin-lane dispatch 托管派工窗口)
```

多行文本(打回问题清单)走 stdin:`laixin-lane send a <<'EOF' ... EOF`

## 循环

**1. 派工(每轨一次只飞一片;每片全新上下文)**
- 核执行总表"进行中"节:该轨有片在飞 → 不发(发车位纪律);
- **判「无片可发」前,先扫总表「⚠️ 已上线缺口」节找「后端已在 main、前端零引用」的条目**(2026-08-17 立,dispatch 十九任样本:排队空转两轮后重扫一次找出 4 条天然纯前端 B 片)——它们是反向假门(能力已承诺、门没装),优先级比新功能硬;缺设计的点名方案窗口/裁定路由细化,⛔ 不自拟射程。**排队节空 ≠ 没活干**,「四个候选都发不了」这类结论只对排队节成立,⛔ 不扩大成对整个知识库成立(与 kickoff 12-bis 同族:窄范围结论不下宽);
- **先 `fresh` 再 `send`**——Codex 上下文不跨片,prompt 自包含(权威库直读);
- **`send` 之后必 `peek` 复核**:见 `Working`/`Explored`/`Read` 才算派工成功;只见提示符=消息被吞,重发;
- **发车前校准占号(机器执法)**:A 轨跑 `laixin-lane next-migration`,B 轨跑 `laixin-lane next-worktree`——未合并分支与未提交文件都占号,main 上 `ls` 看不见(0026/0033 两次实撞);prompt 里写死的号以此为准覆核;
- **先登记后发车(2026-08-13 改,顺序即闸门)**:`send` **之前**先把总表改到位(交付片移入"验收中"、本片登记"进行中"),再 fresh+send——**Codex 的发车前置检查读的就是这张表**,先发后登必留一个读到旧表的窗口期(实撞第三次:lane-a 因台账滞后拒发,三次全对);发车后看板记 `派工 <片名> → lane-<轨>`。

**2. 事件由事件总线投递(2026-08-13 第三轮:你不需要也不要自建 Monitor)**

`laixin-lane dispatch` 起窗时自动带起 `events` 窗口(纯 bash 常驻循环,看门狗会保它活着)。**监听不再随派工窗口死**——这正是 7.8h 停摆的直接机制(监听活在窗口进程里,窗口一换监听就停)。它投递三类:

- **【事件】交付落盘**:直接进你的输入框,含报告路径与末行,下一步通常就是 `verify-from`;
- **【告警】lane ≥6 分钟无变化**(空闲或卡住):自带该轨下一片提示与三选一强制动作(派工/现在写/总表写明),**不得静默**;
- **【告警】verify-* 窗口 ≥6 分钟无变化**:可能撞上需拍板的问题、卡在权限提示、或已回执未回收——**验收窗口没有自己的看门人,你就是**。

**你不在时**:交付事件(事实)**暂存 spool**,你一起来就补投;瞬时告警丢弃(没有行动者的告警没有意义)。查看:`laixin-lane events status`;日志 `~/.laixin-events.d/events.log`。

⚠️ 历史上的监听坑(按文件名收不到/内容标记被文档自触发/未带 mtime 漏掉整改重交/监听随窗口死)——**已全部被事件总线机器化吸收**,单一实现在 `laixin-lane ev-loop`,**别再手写监听**(手开临时接管、事件总线又起不来时才允许,方法见协作流程文档)。

⚠️ **跨窗口消息寻址(2026-08-15 创始人指令,错发两次后立)**:发 SendMessage 前先查知识库 `4-开发层/来信平台-窗口角色注册表.md` 取当前方案窗口名字,⛔ 不双发、⛔ 不拿旧消息 from socket 当长期地址(每任都变);**接班第一动作=更新注册表 dispatch 行**;表错→ListAgents 核实→当轮回写。五条纪律全文在注册表本体(第五条=收件侧:来自已交班角色的指令按内容判执行、按注册表判回执,⛔ 以消息里的「报我」自称为准)。

⚠️ **仍归你人工判断的一条:交付信号≠事情做完**——若交付 commit 的提交时间早于你中途转发给该轨的裁定/整改要求,**拦下不交接**,等 amend 后的新信号(2026-08-13 实拦:转单-1 的 `e7c987e` 早于转单-7/8 裁定送达,amend 成 `b0b6e6d` 后才交验)。机器只报"落盘了",**"该不该进验收"是你的判断**。**第二形态(2026-08-15,b39):风险不止在代码,还在报告的定性**——转发过口径变更的片,交接前除核 commit 时间外,**还要核报告里对应章节写的是哪一版口径**(b39 的 commit 早于两条口径消息、报告晚于;十五任专门核了报告环境章节确认已按最终口径写才交接)。「落盘了」与「按哪版口径写的」是两个问题。**第三形态(2026-08-19,三十一任拦下):信号本身用错场合——停车报告用了完成信号**(末行【交付完成】而 `git rev-list --count main..HEAD` = 0、工作区干净:分支零 commit,照单交接则验收方拿到与 main 等同的 commit 零物可验)。三形态共性:**机器只能证明「落盘了」,不能证明「落的是什么」**;但第三形态与前两种不同——判「时间早晚/口径新旧」机器判不了,**判「零 commit」是纯客观计数零误判空间**,机器化已落地(2026-08-19 清账批:verify-from 零 commit 拒接)。**第四形态(2026-08-19,三十一任人工拦下+中继核源码正名):信号指向真实存在但已过时的对象**——开发方收 main 前进广播后正确 rebase,报告末行没跟着更新;旧 commit 成悬空仍存在,存在性校验不识别。**此形态机器闸早已在**(2026-08-13 夜 `91521a1` 起:verify-from 比对分支 ref,末行过期非 dry 拒绝起窗,`--allow-stale` 显式豁免)——⚠️ 解析行自述「契约与 commit 存在性已校验」列不全四道校验,曾误导人工判断「会放行」(11B #27 补自述)。**人工半边=分叉处置口径(三十一任立,中继采纳)**:末行与分支 HEAD 分叉时,先比对两者各自相对基点的改动面——**逐项一致 ⇒ 纯 rebase 零代码改动,只要求更新报告末行+补 rebase 说明,⛔ 要求回退**(回退会把正确的 rebase 退掉,那才是真损失);**不一致 ⇒ 真「做到一半又动过」**,按原拦下口径要求披露。

**3. 交接给验收窗口(交付到达时)——一片一个,验完即弃**

优先用一步式(解析报告末行取分支与 commit,**防手抄 hash 笔误**,顺带机器校验交付契约与 commit 存在性):

```bash
laixin-lane verify-from "<交付报告绝对路径>" --prompt <prompt绝对路径> --to dispatch
```

或手动全参(报告末行不合契约、需要覆写时):

```bash
laixin-lane verify <片名> \
  --branch <分支> --commit <HEAD commit> \
  --report <交付报告绝对路径> --prompt <prompt绝对路径> \
  --to <你自己的会话名>          # 回执发给谁;不传则它自己 ListAgents 找
```

脚本会:在 tmux `laixin` 会话新开 `verify-<片名>` 窗口 → 起一个**全新 claude**(`--permission-mode auto` + 工具白名单/黑名单)→ 自动过信任与 MCP 提示 → 把五项客观信息派进去。约 15 秒返回。

- 复核:`laixin-lane peek-v <片名> 30`;观看:`laixin-lane attach`;列在跑的:`laixin-lane vlist`;
- 收到回执并合并后:`laixin-lane vdown <片名>` 回收(与 worktree 回收同理,不留残窗);
- 整改重验:先 `vdown` 再 `verify`(同名窗口不重复起);
- **交接后立刻释放主树**:`git -C /Users/pingxia/来信平台 checkout main`——验收窗口走独立验收树,不依赖主树分支;该片 commit 已落地、分支保留,切走不丢;打回时再 checkout 回原分支整改。**不切,"验收中不占发车位"在现场不成立**——台账说不占位、主树看着占位,**Codex 信现场,且它是对的**(2026-08-13 建库路径修复因此连拒两次)。若报 `fatal: 'main' is already used by worktree ...`:**空闲 worktree 占住了分支,先回收它**——回收 worktree 的真实理由是解发车阻塞,不是省磁盘。

⚠️ **git 写操作必须显式 `-C <仓库绝对路径>`,⛔ 依赖 cwd**(2026-08-19 立,三十任实撞):派工窗口**结构性地**同时操作两个 git 仓库(代码库+知识库 vault),台账工作必然把 cwd 带进 vault——实撞形态:校验命令带了 `-C 来信平台`、执行命令没带,`update-ref` 落进 vault 仓,**校验与执行作用在两个不同仓库而在操作者眼里是一套连贯动作**(「两个真相,一个错误」);没出事纯因该 commit 在 vault 不存在被 git 拒写。**校验与执行必须同一个 `-C`**。与「合并前先 `git branch --show-current`」分层:那条防**分支假设**,本条防**仓库假设**。

⚠️ **绝不允许两个派工窗口同时活着**——两个窗口同时动 lane 会把 Codex 上下文搞乱(2026-08-13 实际险些发生,当时靠人工提醒才没撞)。**机器执法方式:派工权锁**。

- 接管派工角色**第一件事** `laixin-lane claim`;此后每次 `send` **自动续期**;
- `dispatch` 起窗时脚本代它认领;别的窗口来 `claim` 会被拒(除非持有者停止动 lane 超 30 分钟,或显式 `--force`);
- 看门狗在"dispatch 已死但派工权被别人新鲜持有"时**不自动重起**,只告警——那说明有人接管了。

> **为什么不用"数 tmux 外的活跃会话"来判**(曾如此实现,已换掉):数量是身份的**不可靠代理**——你开一个窗口只是问问题、和开一个窗口接管派工,在"数量"里长得一模一样。**基线设低会误判罢工,设高会在真撞车时不拦。** 而 `send` 是派工窗口的**定义性行为**:谁在动 lane 谁就是派工窗口 ⇒ **直接测行为,不猜身份。**

⚠️ **跨窗口移交的每条结论,都要附上得出它的那条命令**。2026-08-13 一天内双向各发生一次"把自己的推断当事实移交":①交接窗口把"商会申请审核状态"整个判为缺口,接班窗口核出接口早已返回;②接班窗口把 `categories.clarification_questions` 判为"字段挂错对象",而 `Category` 类恰好定义在 `demand.py` 文件里(按文件名推断会误读),复核后原文正确。**两次都是读代码得出的结论与代码本身之间那层推断,在传递中丢掉了不确定性标记。** 附上命令,接班方就能一秒复核;不附,它要么盲信、要么重查一遍。**接班窗口重验前任判断是应当鼓励的行为,不是不信任。**

⚠️ **交接只传那五项,不得附带你的判断、预期或"应该没问题"**——你写了 prompt,你的预期就是污染源。脚本发的指令模板已经是干净的,**不要手动追加评论**。

**为什么一片一个窗口**:验收是上下文消耗最大、也最容易凭印象书写的环节(2026-08-13 夜间编造事件全部发生在单片验收内部)。一次性窗口把"靠换班纪律压住"变成"结构上无法积累",与 Codex 每片 `fresh` 是同一条纪律。

**4. 交接的同一轮内,立即派该轨下一片**——**合并不是终点,派工才是**。验收期间该轨空闲是事故,不是等待。

**5. 收回执后收尾(每步记看板)**
- **通过**:回执含 `verify-*` 分支、其 commit、**所基于的 main commit**、全量结果。合并前**两道校验**:①`laixin-lane evidence <片名> <数字>` 复查——**回执里出现的每个数字都要能在证据里命中,对不上即退回重验**(2026-08-13 编造事件的机器化防线)。⚠️ **复查必须走工具的搜索通道 ⛔ 裸 grep**(2026-08-19 补,三十任实撞):证据日志常含 Codex 转义路径的非 ISO 字节/NEL 终止符,**裸 grep 判非文本静默零命中——「证据不存在」与「证据在但 grep 读不了」输出同形,而纪律动作是退回 ⇒ 缺陷的表现恰是让合规验收被判不合规**;工具通道自带 `grep -aF` 穿透并在零命中时提示核 file/wc(08-14 同族实撞后已内置,源码注释有案)。通则(三例归纳:tail 被噪声顶掉/head 抓到提示符/本条):**单一探针的零结果不直接变成结论,换一种探针再看一眼**——四形态「探针翻转」在合并侧的应用;⭐ **细化(2026-08-14)**:grep 不到的条目,先判它是不是**你能独立复现的客观事实**(如纯 git 对比)——能复现就自己复现替代(复现即比 grep 更强的证据),复现不了才退回;既守住「可核」的线,又不为 30 秒能自证的事退回整轮验收;⚖️ **本条款射程边界(2026-08-18 中继裁,R3 载荷半边案)**:复现替代治的是**验收/合并方的取证成本**,⛔ **不覆盖交付方的披露义务**——prompt 不可协商条款要求的自证行/清点节(commit 数达标行、拆防线余防清点等)缺失时,**别人替它跑出实质不等于它披露了**;尤其自证行的全部意义=逼真实的未达标自己说出来,「工程全绿时豁免披露」恰恰豁免在了披露规则唯一的生效场景上,判据写死后第一次咬人就豁免=判据变装饰。此类缺项=工程打回(纯文档整改轮,不升级不挂起);②**校验 `main` 仍等于回执里的 main commit**;已变动 → **退回验收窗口重跑**,不得直接合。合并 → **四处台账**(log/执行总表片级状态/模块台账/**执行总表排队节该片行整行移出**——⛔ 标记不算移出,2026-08-14 三行陈旧排队行连误三方后裁定)→ **第五处:事实表刷新**(2026-08-18 补,实测 7 次合并零执行——刷新说明只写在生成物表头上,「靠自觉的步骤在流水线里等于不存在」):跑 `laixin-lane facts-fresh`,报过期即**当轮重跑生成器**(命令在表头与 facts-fresh 输出里;导出器 ref 级零碰在飞轨道随时可跑,codegraph sync 由它顺手跑或主树被占时告警)——漂移代价已实证:表落后 7 片时消费点表差 773 行,人工枚举与陈旧表**各有遗漏谁也救不了谁**,而「人工枚举要对表、表说了算」的前提是表得是新的 → **台账改完当轮 `laixin-lane kb-commit` 提交总表精确路径**(2026-08-19 补:总表整晚不提交=任何误改**不可回滚**,一次真实丢失已发生;kb-commit 顺带触发分桶自检与 facts-fresh 两钩,一动作三防线)——⚠️ **恢复警告扩写(二十九任「不能 checkout」的一般化)**:未提交期间,**任何以 vault git HEAD 为基准的恢复动作**(checkout / git diff 删除侧)都不可用——**HEAD 不是「我改之前」,是「上次提交之前」**,用它恢复会连带丢掉之后所有正确改动;机器化候选(laixin-lane 合并后自动提交)归 11B 与 #14c 同批 → 看板记 **`合并 <片名> <commit> <用例数>`(格式成文,2026-08-19:audit-queue 强嫌疑分级按「合并 <片名>」紧邻匹配,片名紧跟「合并」是机器判据的一部分 ⛔ 自由改写)** → 回收该片 worktree;
- ⚠️ **ref 级合并的前提=没有工作树 checkout 在被移动的分支上(2026-08-19 立,三十一任实撞后中继落卡)**:`git update-ref` 移动 main 指针**不会**同步已 checkout 该分支的 index 与工作树 ⇒ 主树 HEAD 新、内容旧,`git status` 呈现旧 main 对新 main 的**反向差异**,A 轨硬闸门会因此停车(闸门在设计场景之外正确生效,开发方照停是对的 ⛔ 不得因「看起来只是几个前端文件」自行 reset)。**规矩**:每次 ref 级合并(`update-ref`/`branch -f`)前核 `git -C /Users/pingxia/来信平台 branch --show-current`——**等于被移动的分支时,合并后当轮同步主树**:先核三件(staged 差异方向恰为本次合并的反向/工作区与 index 一致/无未跟踪文件)确认丢的只是旧内容,再 `reset --hard` 到新值;三件任一不成立=可能有真实工作,停手排查 ⛔ 盲 reset;或改用普通 checkout+merge。**陷阱形态**:两个各自正确的做法(交接后按纪律释放主树回 main × ref 级合并不占主树)**组合起来才出问题**——此前主树总停在在飞片分支上,条件从不成立所以从未暴露。机器化候选已登 11B #24;
- **打回**:把验收窗口给的问题清单原样 `send` 回该轨(Codex 在原分支整改)→ 看板记 `打回 <片名> <一句话原因>`;**座位裁决(2026-08-13)**:打回整改进该轨**队首**(优先于一切排队片),但**不中断已开工的在飞片**——在飞片交付进验收后,座位先给整改(例外:整改片是在排片的阻塞性依赖且创始人在线,可请示中断;**夜间一律不中断**);
- **挂起**:**打回分两类,挂起判据不同(2026-08-13,分类权在验收窗口回执,派工窗口照执行、不自行改判)**——**方向性打回**(方向性问题/裁定缺口/红线争议/验收方判"需升级")**一次即挂起**;**工程打回**(实现缺陷、整改新引入的回归,验收方判"不需升级")可继续整改,**同一片累计三次才挂起**(防无限循环)。挂起 → 看板记 `挂起(<类型>)` → SendMessage 报方案窗口;⚖️ **措辞修准(2026-08-19,方案窗口第十任确认按实质执行后落卡)**:方向性打回**一次即挂起该片**——整改进该轨队列,**⛔ 中断该轨无关在飞片**(铁律立意=防带病方向继续产出;被打回的片已交付离轨,停一个零交集的在飞片**不减少任何风险,只损失产出**——原文「挂起该轨」写成时打回片=在跑片同指,并行度上来后两者才分开,字面就误导了);字面与实质冲突一律上报,不自行解释规则。

---

# B. 验收窗口

**你由 `laixin-lane verify` 起,一片一个窗口,验完即弃。** 收到派单后:

1. **先重读该片 prompt 与执行总表在飞节,再读交付报告**——不依赖窗口记忆(可能已被压缩);看板记 `验收开始 <片名>`;
2. 按 `laixin-acceptance` 七步独立验收:**全量自己跑、browser-harness 自己走查、报告没提的验证项就是最该亲手跑的**;判据是"全部 passed 且 zero failed",**不是测试数相等**(subTest 计数不稳);
3. **验收隔离**:主树可能被 A 轨 Codex 占用,一律走独立验收 worktree 或 ref 级操作(`commit-tree`/`branch -f`),**禁止在共享工作树 checkout/merge**;
4. 小口子自行修复并在验收记录写明;方向性问题打回;
5. **最后一步必须是:rebase 到当时 main,并在 rebase 后的状态上复跑全量**——"验的就是合的";
6. `SendMessage` 回派工窗口:
   - 通过 → `verify-*` 分支名 · 其 commit · **所基于的 main commit** · 全量结果;
   - 打回 → 逐条可复现的问题清单 + **打回类型(方向性/工程,分类权在你)** + 是否需升级创始人;
7. 在同一份记录文件追加"验收方独立验收结论"节;看板记 `验收通过` 或 `打回`。

**你不自行 `laixin-lane send`**,即使判定打回也经派工窗口转发——两个窗口同时对一条 lane 发消息会造成 Codex 上下文错乱。

**回执发出后你的活就结束了**——不要找新活干,等派工窗口 `vdown` 回收你即可。(历史片的"零命中"结论复核是另派的独立任务,不是你顺手做的事。)

**工具层已给你上的锁**(不必自觉,也绕不过):`git push` / `git merge` / `git reset --hard` / 一切 `laixin-lane send|fresh|up|down` 均被 `--disallowedTools` 硬拒。记看板用 `laixin-lane vlog "..."`(**不要写 `LAIXIN_WINDOW=... laixin-lane log`**——带 env 前缀的命令不匹配白名单,会平白卡一次权限提示)。

---

## 上下文换班纪律(按证据量分档)

- **Codex**:每片 `fresh`,不存在积累问题;打回整改不 fresh(分支现场就是它的记忆);
- **验收窗口:一片一个,不换班也不复用**(`laixin-lane verify` 起、`vdown` 收)——一次全量输出+一份 diff 复核就能吃掉可观上下文,2026-08-13 夜间的编造事件正是连续验收多片后发生的;一次性窗口让上下文结构上无法积累;
- **派工窗口:换班窗口 = ctx 55-70%(创始人 2026-08-18 裁定:「dispatch 换得太勤,没到 55% 就换掉了;55-70% 这个窗口再换」)**——换班本身有成本(交接包+新任热身+台账重核,二十一任曾一轮推翻前任 5 条登记),⛔ **ctx <55% 不主动交班**;四轨判据的角色相应调整:

| 轨 | 判据 | 性质 |
|---|---|---|
| ① 上下文 | `laixin-lane ctx` **≥70% 硬闸门必须换**;**55-70% 为换班窗口**(在此区间结合②④择机换);**<55% ⛔ 不主动换** | 留 30% 给交接与收尾;55 下限=创始人 2026-08-18 裁 |
| ② 片数 | 派工 8-10 片 | **降为择机参考,⛔ 不再独立触发换班**——55% 以下片数到了也继续干,进 55-70% 窗口后作为「该找收尾点了」的信号 |
| ③ 行为信号 | ⓐ 开始用"重新查证"代替凭记忆;ⓑ **记不清某数字来自哪次工具返回** | ⓑ **出现即交,不受 55% 下限约束**——编造的直接前兆,安全例外;窗口死亡/冻结被看门狗换掉同理不受限 |
| ④ 错误信号 | ⓐ 台账出现遗漏或前后不一致;ⓑ 被下游(Codex/验收窗口/接班窗口)纠正过判断 | 判断力下滑的证据;<55% 时记录在案不触发,进窗口后与②合并择机 |

  ⚠️ 55/70 两个数与 `laixin-lane ctx`、`statusline.py` 的既有阈值**数值相同,无需改码**——变的是语义:55 从「预警线」变「换班窗口下沿」。

  ### ⛔ 报占比前必须真跑 `ctx` —— 不许估,不许说"实测"除非刚跑过(2026-08-13 晚立)

  **失败样本(方案窗口自撞两次,第二次比第一次严重)**:

  | 我报的 | 实测 | 我当时怎么说的 |
  |---|---|---|
  | 66% | **41.7%** | 直接写在汇报里 |
  | 68.2% | **52.1%** | **明确写了"这次是实测读数,不是估的"** |

  ⇒ **第一次是凭感觉,第二次是凭感觉并声称不是凭感觉。** 而这个数字是**交班闸门的唯一判据**,报错它等于闸门失灵。

  ⚠️ **注意这与下一条的区别**:下一条讲的是"感觉不准"(自我感知有偏差),**本条讲的是"根本没跑就报数"** —— 后者不是精度问题,是**把估计当成了测量**。⇒ **规矩:凡在任何输出里写出上下文占比,当轮必须先跑 `laixin-lane ctx <你的 session 前 8 位>`;没跑过就不许写数字,写"未测"。**

  ⭐ **同族扩展(2026-08-14 凌晨,dispatch 第六任自撞后立)**:**时间戳也是可测量**。往台账写时间戳,当轮必须先跑 `date`——第六任在总表手写的几个 `02:xx` 全是推算的,实跑 `date` 发现偏晚 20-40 分钟。⇒ **通则:凡有一条命令能测到的量(占比 / 时间 / 处数 / 测试数),写进任何台账前当轮实测,⛔ 不推算、不凭感觉、不抄上一轮**——"处数"那半已另有实撞(76/78,见 kickoff 第 13 条第 3 小条)。

  ### 🖥️ 人看状态栏,窗口跑 `ctx` —— 两条路,别混

  2026-08-13 晚新增了 Claude Code 状态栏(`~/.claude-official/statusline.py`,配在 `~/.claude-official/settings.json`),实时显示占比、闸门色、session 前 8 位:

  | 谁 | 用什么 | 为什么 |
  |---|---|---|
  | **创始人(人)** | **状态栏** | 数据由 Claude Code 直接传给脚本(`context_window.used_percentage`),**不经过窗口的自我报告** ⇒ 上面那两次谎报,人一眼就能戳穿 |
  | **执行窗口(模型)** | ⛔ **仍然只能 `laixin-lane ctx`** | **窗口看不到状态栏** —— 它渲染在人的终端里,不进模型上下文。**别以为装了状态栏窗口就能自查了。** |

  ⚠️ **闸门线现在有两处定义**(`statusline.py` 的 `GATE_HARD/GATE_WARN` 与 `laixin-lane cmd_ctx` 的 `70/55`)⇒ **改一处必须同步另一处**,两边代码里都写了提示。**这是已知的双真相源,不是疏忽** —— 一个是 bash 一个是 python,跨语言共用常量的代价大于收益。

  ⚠️ **不能用 `/usage` 的 "cache write" 反推上下文**(2026-08-13 两次实测,方向相反):
  | 读法 | 得数 | 实际 | 偏差方向 |
  |---|---|---|---|
  | 从 `/usage` cache write 反推(样本一) | "约 50%" | 54.9% | 偏低 |
  | 把 `/usage` cache write 当成当前上下文(样本二) | "快 700K" | 44.5% | 偏高 |

  **`cache write` 是整个会话的累计写入量**(一次会话 377 次请求,每次写一点,累计 998k——已超过 1M 窗口本身,足以反证它不是"当前装着多少")。当前上下文只有一个来源:**transcript 最后一条 `usage` 的 `input_tokens + cache_read_input_tokens + cache_creation_input_tokens`**——三者互斥,相加即本次请求总输入。`laixin-lane ctx` 存在的理由就是这个。
  ⚠️ **多窗口环境下 `ctx` 不带参数会取错**:它按 mtime 排,而本机同时跑着方案/派工/验收多个会话。**查自己一律 `laixin-lane ctx <你的 session 前 8 位>`**(在 scratchpad 路径里)。不带参数时它会列出所有会话让你认领,不猜。

  ⚠️ **绝不能凭感觉判断该不该交班**。实测(拿一个真实派工窗口做样本):自评"30-40%",`/usage` 反推"约 50%",**transcript 精确读数 54.9%** ⇒ **自我感知低估 15-25 个百分点**。感觉只能触发检查,**判据必须是 `ctx` 读数**。
  ⚠️ **①②必须并存,它们管不同的风险**:顺利时纯派工约 34k/片,8 片才吃 27%,这时片数闸门先到;但事故(误判调查、网页查证、复核)能让 4 片就烧到 55%。**片数管常规节奏,占比管意外——而事故恰恰在长会话里更容易发生,只靠片数计数器,它会在最该报警时走得最慢。**
  **交班动作**:每合并一片后 `laixin-lane ctx` 自检 → 命中即**先把状态写完整**(总表进行中 + 看板轨道快照 + log)→ 写交接包 → `laixin-lane dispatch --fresh` 起新窗口 → **确认新窗口已接管后才收工**;
- 任一窗口自觉开始翻旧消息找状态,就是该换班了。换班时:看板记 `换班`+更新"轨道快照"节。**若派工窗口已被看门狗托管,换班不需要人**——`laixin-lane dispatch --fresh` 即可换全新上下文,看门狗也会在它死后自动这么做;
- **新窗口起手式**:`laixin-lane doctor`(体检,会查事件总线在不在)→ 调 Skill laixin-pipeline → 认领角色 → `laixin-lane stats`(燃料+片级周期)→ 读执行总表"进行中"+看板末 20 行 → 接管。**不需要重建 Monitor**——事件总线独立于窗口存活,你不在时的交付已在 spool 里等你;
- **交班时先 `laixin-lane handoff`**:客观半部(main/拓扑/验收窗口/派工权/事件总线/燃料/周期/看板尾)机器生成,**你只写判断——且逐条标明是判断、附得出它的命令**;
- **方案窗口**:设计落库即窗口可弃,接续走记忆+index.md。

## 夜间无人值守(创始人睡觉,流水线照跑)

> **2026-08-13 实测教训**:流水线建成后的头 11.4 小时里,**8.4 小时(73%)是静默的,最长一段 7.8 小时正好是创始人睡觉时间**。根因不是纪律松,是**整条流水线的存活依赖"有一个活着的窗口"**:上一个窗口写完换班快照就收工了,而"开新窗口"只有人能做。**轨道空闲告警再完善也没用——告警发给了一个已经不存在的窗口。**
> 解法是看门狗(`laixin-lane watchdog`):纯 bash 常驻循环,不累不崩没有上下文,是整条流水线里唯一不会忘事的东西。派工窗口死了它重起,叫不醒它先戳后换。**但它只解决"没人派",解决不了"没片可派"**——夜间能跑多久取决于队列燃料,用 `laixin-lane stats` 看。

- 只做**排队节已 ready** 的片;prompt 没写好的不现场发挥;
- 工程打回可继续整改(同一片累计三次才挂);**方向性打回或任何裁定缺口一次即挂起该片**(⛔ 中断该轨无关在飞片,同上节措辞修准;该片整改夜间不发=不代拍裁定、不吵醒创始人,该轨其余 ready 片照跑);**夜间一律不中断在飞片**;
- 合并照常(验收全过才合,标准不因夜间降低);
- 早晨交班:看板事件流就是夜报,方案窗口向创始人汇总"合了什么/挂了什么/为什么"。

## ⚠️ 模态对话框=自愈体系盲区(2026-08-15 新增;「Set up auto mode」一天冻 4 窗:dispatch 两次 74min+5.5h、两验收数小时)

> **为什么看门狗全程没救**:冻结时窗口「活着」(进程在、窗口在),文字戳被对话框吞掉;更隐蔽的是**戳窗引起的画面重绘会重置静默计时** ⇒ 永远到不了重起阈值;即便重起,新会话照样弹。**「活着」与「活着但被冻」在看门狗原有判据下不可区分。**

三层防线(均已落地,恢复流水线前无需再配置):
1. **看门狗 dialog_sweep**(主防线):每轮巡检 dispatch 与全部 verify-* 窗口画面,签名匹配已知对话框——已验证安全键的(auto-mode 设置/向导)自动 Esc 清除;**trust 对话框与未知对话框只告警绝不动键**(trust 上 Esc=杀进程,2026-08-13 实撞)。签名库=`dialog_classify`,可独测:`echo "画面文本" | laixin-lane dialog-classify`。**新增签名前必须人工实测该对话框的安全键再入库。**
2. **流水线窗口专用 settings**(兜底):起窗自动挂 `--settings ~/.config/laixin-lane/headless-settings.json`(`askUserQuestionTimeout: 60s`)——即使新型对话框漏过签名库,60 秒后自动继续,冻结有界。⛔ 该项不进全局 settings.json:方案窗口是人机对话,问句不该被自动应答。
3. **人工处置口径**:见到窗口卡在对话框,先读对话框自标的键位说明再动键;**⛔ 对着「Enter to confirm」的三选框按方向键+Enter**(方向键可能不生效,Enter 落在默认项上——2026-08-15 实撞误入设置向导)。

## ⏸️ 暂停封口序列(2026-08-15 新增;Codex 额度耗尽首次暂停实走后固化)

触发:创始人说暂停(额度耗尽/主动停机)。**顺序不可换**:
1. **⛔ 停发新片**(零 fresh/零 send);「在做的可以做完」= 在飞片与一次 send 可收尾的停车片允许跑完交付;
2. `laixin-lane watchdog stop` **先单停看门狗**(消「轨道空闲」告警——暂停期空闲不是事故;⛔ 此时不可 halt);
3. **在跑验收照常走完**(验收/合并是 Claude+git,不耗 Codex 额度,标准不降)→ 回执收齐 → 该合的合;
4. **暂停基点写进执行总表**:main hash/两轨归位/燃料存量/**恢复起点一句话**——恢复时的窗口可能不是你,账要自己会说话;有成本决策数据(如 fast mode)一并记看板,**可测量与推断分开写**;
5. **最后才 `laixin-lane halt`**——它停三件套(watchdog+events+**dispatch 窗口自身**),lanes/verify 不动 ⇒ 它必须是 dispatch 的最后一个动作,且 events 一停交付信号就断,**⛔ 回执未收齐前不可执行**。
- 恢复:`resurrect --infra`(只起 events,不烧钱)→ 额度确认后 `resurrect --full` → 按暂停基点「恢复起点一句话」续。

## 读屏深度纪律(2026-08-15 新增;同一窗口 tail 6 行判「空闲」、peek 30 行见完整交付段——结论差一片)

判断轨道/窗口状态,**capture 深度 ≥30 行**(`laixin-lane peek <轨> 30`);⛔ 用 tail 数行的空提示符下「空闲/没干活」结论——Codex 交付段、Claude 回执段都在提示符上方,浅读=只看到句号没看到正文。

## 窗口间通信

- `ListAgents` 找对方,`SendMessage` 互发;方案窗口发**设计与裁定**,派工窗口回**片级事实**(合入 commit/用例数/挂起原因),验收窗口只与派工窗口对接;
- **任何监听事件或消息到达,必须在同一轮内完成「判断 → 行动 → 可见输出」**;只调工具不产出可见结论,在外部看来与卡死无异;
- 创始人人工消息(attach 或任一窗口直接发话)与流水线消息等效,人工优先。

### ⚠️ 口径消息到达时,当轮扫存量(2026-08-18 立,一次方向性打回后由 dispatch 与方案窗口各认一半责任)

**样本**:方案窗口 12:4x 为新片定词时裁「⛔ 用后端那句『5 分钟内已加入该服务,请二次确认』」;而 dispatch 12:30 已发出的另一片 prompt,其主判据要求渲染的**正是那句**。交付 13:08、词表落库 13:12 ⇒ 验收方按 commit 时间戳判「双方都无从得知」,**但 dispatch 自查后不接受该免责**:那条裁定消息 12:4x 就到手了,**中间有二十多分钟可以给在飞轨道发一条口径更正**。片工程零缺陷、全量全绿,仍被方向性打回。

⇒ **两侧各一条,缺一条都不闭合**:

| 侧 | 规矩 | 它覆盖什么 |
|---|---|---|
| **发出方(方案/裁定窗口)** | 凡裁定**推翻此前已发出的口径**,消息**首行**标 `⚠️ 在飞口径变更:推翻 X,受影响 prompt/片=Y` | 发出方**知道自己在推翻什么**的情形 |
| **接收方(派工窗口)** | 收到任何口径/裁定消息(**⛔ 不论有没有那个标记**),**当轮**扫一遍「在飞片 + 已写好未发的 prompt」有没有引用被推翻的东西,**扫完在看板留一句结论,哪怕是「零命中」** | 发出方**不知道自己在推翻什么**的情形(副作用推翻) |

⭐ **为什么必须两条**:本案那条消息在发出方看来是「**给新片定词**」,推翻旧口径只是副作用 ⇒ **标记依赖发出方自觉,扫存量依赖接收方动作**。只立发出方那条,副作用推翻仍会漏;只立接收方那条,接收方要在每条消息里自己发现「这是推翻」。

📌 **这是 kickoff 第 8 条第 5 小条「新事实到达时,已发出的 prompt 已经变成带病存量;上墙不等于生效」在流水线侧的落地动作** —— 那一条讲的是自觉,本条把它变成一个有产出物(看板那句结论)的动作。

⚖️ **扫存量=双层扫(2026-08-18 深夜升级,方案窗口第十任裁,dispatch 二十九任实撞后提)**:①**文本层**——引用被推翻口径的 prompt 行;②**文件层**——新裁定的**落点文件**与在飞/ready 片**改动面的交集**;**文本零命中 ⛔ 当零冲突结论**(实撞:五份 prompt 关键词命中 0 行,而新裁定要动的两个守护测试文件恰与同轮刚写的 api 片改动面同目录同族——按文本层收工,两片会并行改同一个测试文件族)。**落点文件需实际追出**(裁定文本常不含文件名——本案是 grep 守护头注才找到的),⛔ 只比对裁定字面提到的路径。**文件层命中的处置=负面清单划界,并把界写成两片各自的判据**;⭐ **冲突资产化**(方案窗口命名):划界后重叠文件从风险变成对方片的判据——本案 api 片被禁动两个测试文件、新增测试落新文件,**同时要求既有断言保持绿**,那正是它「只加不改」是否成立的最强现成证据。

## ⚠️ 台账书写八律(2026-08-14 立四律;2026-08-18 补第五至七律;2026-08-19 补第八律;各有多次实撞,机器不报错只静默出错)

1. **状态标记前置于状态格首个 `<br>` 之前**——`stats` 分桶与 `ev_next_ready` 只认首段,追加在末尾等于对机器不可见(实撞≥2 次);
2. **表格行内⛔任何形式的竖线**——`grep` 模式串的 `\|`、代码里的 `||` 都会把行劈开且不报错;**要引用代码一律改写成中文描述,⛔ 不粘贴代码片段进表格行**(同一人同日两撞后收紧);
3. **交付/挂起/合入的片整行移出该节**——删除线与标记都不是移出,Codex 判发车位、机器分桶都按「片名在哪节」(实撞 3 次,含 87% 说谎率的排队节);
4. **分桶判据读源码,⛔ 不从行为反推**——实撞:「prompt v2 已重写」不含判据串 `'ready'`/`'prompt 已写'`,stats 报 0;前两次靠反推蒙对,第三次蒙不中才读源码。**写完台账跑一遍按节格数体检 + stats 复读自己刚写的行,并打印四节片名清单与实际轨道/验收窗口逐一对照**(2026-08-18 补,三十任实撞:整改片交接进验收后忘移行,进行中节挂着两片而 A 轨实跑一片——该行 4 列无竖线无禁止符**格式完全合法,只是在错误的节里**,格数体检与 stats 都不报错,而进行中节是 Codex 判发车位的唯一依据;同一份台账刚连做三次移行、第四次就漏,**修过、执行过、刚讲过,都不构成免疫**——格式体检抓格式错,节归属要靠清单对照)。⭐ **「读源码」的完成判据(2026-08-18 补,dispatch 二十六任实撞后自析)**:函数体里用到的每个变量都要**追到它的赋值处**——只读函数体看到 `'⛔' in st` 就把 `st` 当整个状态格,而赋值处逐字是取首段;**只读函数体等于只读了一半,且「核了一半」在自我感觉上和「核完了」完全一样**(比「没核」更隐蔽:核过的自信会挡住第二遍核)。与 kickoff 12 探针形态族同源。
5. **✅ 只能标到单功能粒度——一格装多个功能必须逐项标**(2026-08-18 补,dispatch 二十三任实撞:「复制链接/二维码 ✅」一格两功能而 ✅ 只对前半,四探针实测二维码从没做过;不核就会写出「复用不存在实现」的 prompt)——半真的 ✅ 比 ⬜ 更危险:⬜ 让人去查,半真 ✅ 让人直接引用。
6. **台账文本防 shell 求值**(2026-08-18 补,dispatch 二十六任实撞:heredoc 写看板时文中反引号被当命令替换**求值执行**,两处命令串被吃成空串——记录看起来完整、实则丢了它全部价值所在的证据命令,且工具照回「已记录到看板」):写盘走**单引号 heredoc**(`<<'EOF'`)或脚本写盘,⛔ 把带反引号/美元号/未加引号竖线的长文本塞进命令行参数;**「已记录」的回显只证明写了,不证明写对了——写完复读自己刚写的行**(与第 4 律收尾动作同一句,两律共用)。此前 log 反引号坑(二十三任 08-18 记)是本律的参数形态,本律把 heredoc 形态一并收进。
8. **定位台账行禁用包含匹配——一律行首匹配 `行.startswith('| **<片名>')`,⛔ `片名 in 行`**(2026-08-19 立,三十任一轮双撞并致一次真实数据丢失):片的状态格经常提到别的片名(排期链/邻接提醒/main 前进广播),包含匹配**命中了,命中的是错的东西**——一次把红线级已完成记录改成「已发车」移进进行中,一次覆盖了前任补登整格(原文已丢失,重建依据在行内明写)。**配套两半**:①体检加一步「全片名包含匹配数 vs 行首匹配数对照」,不等即有误命中风险(实测 3 个片名带险,一次看清;⭐ 三十一任再测:四片名包含匹配数 6/4/3/4 而行首匹配全部唯一——包含匹配定位四行会误中三行);②**发现一个探针 bug,当轮排查它的全部历史使用,⛔ 只修当场那处**(第二处因此又躺半小时)——与合并校验的「单一探针零结果不成结论」是一对:那条管探针的**结果**,本条管探针的**用法**;与搬运脚本「命中数非唯一即中止不写盘」同族(总表交接包既有)。
7. **禁止符号是机器控制符——执行总表表格行内一律不用 ⛔,要表达禁止改中文否定词(不/勿/禁止)**(2026-08-18 补,dispatch 二十六任实撞:登记行里几处禁止说明用了 ⛔,整行被 stats 判「前置未解」不算燃料,燃料 19 非 20)。**机器精确语义(中继当轮读源码,写给较真的人)**:内容格(第三列)**整格任意位置**出现 ⛔ 即拦;状态格**仅首段**拦——但书写规则**从严按「整行不用」执行**,⛔ 依赖「状态格后半可用」这类实现细节:它会随下次重构翻,而今天已两次有人把实现细节当规则记(先记成「仅首段」、再记成「整格」,各错一半)。⭐ 机器这次的正面设计留档:被拦行列名给人核,5 分钟定位——「不拿误报换漏报,首选列名」原则的实战样本。⭐ **从严版首个无报错生效样本(二十九任)**:状态格后半写了 ⛔,stats 实测没拦(机器只看首段),仍按本律清零——**规则在机器不报错处生效,正是「不依赖实现细节」的价值所在**。

## 方案窗口协作与排位(2026-08-14 新增,第五任立;创始人"要形成机制"指令的流水线侧)

1. **缺设计片点名即补,不绕创始人**:凡要发的片没有设计托底(台账【无设计💡】或写 prompt 时发现框架要自己发明),⛔ 不硬写——SendMessage 点名方案窗口,方案窗口当班补策划(样本:2026-08-13 两撞"台账指向不存在的设计"白排队;2026-08-14 资金-3 无设计当日补齐销号)。**prompt 走样或框架过大,多数根因是缺策划托底**(创始人原话定性);
2. **排位准则:能改变后续排期的片上位**——盘点/核查类片修的是"未知",功能片修的是"已知";断点清单没出来之前,后面的排期全是猜的(样本:待核 A-D 上位裁决,2026-08-14);
3. **销账写回被引用处**(细则在 acceptance 卡四处台账节,此处只指路):同一行陈旧登记能连绊两个窗口——销账以"它被引用的那些地方"为完成边界;
4. **授权-1/授权-2 指针**:前期判定归方案窗口直接裁(授权-1,标注可回改);**授权-2(2026-08-17 创始人):无原则性问题方案窗口直接判,原则性问题/巨大冲突判「往后做/讨论再做」进暂存,⛔ 不逐项问创始人,流水线不停**——细则均在协作流程"前期判定授权"与"授权升级"节。派工窗口遇裁定缺口,找方案窗口(或注册表所载路由窗口)即可,⛔ 不等创始人也 ⛔ 不自裁。

## ⚠️ 合并即广播:main 前进的那一刻通知所有在跑验收窗口(2026-08-14 新增,dispatch 十二任撞出)

**并行验收 + 合并会互相作废**:同时段三个验收几乎同时完成,合了先到的一个 ⇒ main 前进 ⇒ 另外两份回执全部失效退回 rebase 复跑——**这不是调度失误(先合谁其余都失效),但白跑一遍全量是可省的**。

规矩:**每次合并完成后,立即向所有在跑验收窗口广播「main 已前进到 <commit>」+ 客观变更清单**(改了哪些文件),让它们在收尾前 rebase,而不是跑完全量才发现要重跑;⛔ 广播只给客观事实,不替验收方下"对你无影响"的结论(交接协议"不带判断"同规)。

**广播里引用的数字必须标明取数侧(2026-08-19 补,三十一任实撞后中继落卡)**:「main 的数」要说清是 main 的数,候选侧的数**当场在候选上枚举** ⛔ 拿 main 侧读数代候选——实撞:广播「前端 test 脚本仍是 36」是 main 读数,而验收对象是候选(含该片新增脚本实为 37),被验收方当场纠正;样本已入失败样本总账 B 族。

## ⚠️ 并行验收窗口:走查端口必须专属(2026-08-13 新增,验收方自撞)

**一次性验收窗口启用后,同时在跑三四个验收窗口成了常态**,于是出现了一种此前不存在的故障:

- `verify-消费者页枚举收口` 起走查服务时,**3100 被另一个验收窗口占着** ⇒ 它的 `next start` **静默退出**;
- 浏览器仍能打开 3100 —— 但那是**别人的服务**,页面内容是 **main 版前端**,不是它要验的分支;
- ⇒ 它**差点据此多报缺陷**。自己发现后作废了整段证据(留档 `walkthrough-invalid-portclash.log`),改用专属端口 3177/8177 重跑。

**这个故障的形态和 b17 那次 CORS 撞端口是同一族**:**页面"看起来正常打开了",而你验的根本不是你以为的那个东西。** 区别是这次的假象更强——不是空页面,是**另一个分支的真实页面**。

**规矩**:
1. **每个验收窗口用自己的专属端口**(建议按片名派生,如 3177/8177),**不要用 3000/3100 这两个默认口**;
2. **起服务后先自证"这是我的分支"** —— 比如取一个只在本分支存在的字符串,或核 `/api` 某个本片新增字段;**只看到 200 不算**;
3. **端口撞了要能看出来**:`next start` 撞口是**静默退出**,所以起服务后必须检查进程还在不在,不能只看 curl 通不通;
4. ⚠️ 后端同理 —— **端口要与 `cors_origins` 放行一致**(默认只放行 3000/3100),用专属端口时记得一并配 `CORS_ORIGINS`,否则会退化成 b17 那次的"token 被清空、页面全空"。

## ⚠️ 走查用专属 headless 浏览器,别抢共享 Chrome(2026-08-14 新增,同日两验收方撞同一根因)

共享 Chrome 争用(约 20 个并行标签+本机双版本)会卡死浏览器走查,且**流水线不能依赖人点 Allow 弹窗**。规矩:验收/走查一律用**专属 headless Chrome+独立 daemon 名**(与"走查端口专属"同族——端口专属、浏览器实例也专属);样本:确认换人页验收卡弹窗、SSR 验收方用 9331 绕开。

## ⚠️ 采集前断言当前标签页 URL——端口专属与浏览器专属都挡不住 daemon 串台(2026-08-17 新增,同日两验收方独立实撞)

「专属实例会被 daemon 复用架空」(验收方原话):漂的不是端口也不是浏览器进程,是 **daemon 的标签归属**——`ensure_daemon()` 会忽略 `BU_CDP_URL` 复用别人的 daemon,标签被切到并行验收窗口的页面上。两个当日样本:①通知片验收方标签被切到 `127.0.0.1:3246`,首次采集作废;②主页-3 验收方已起专属 headless(9347+独立 profile),daemon 仍被安全片窗口(3178)复用,一次测量跑在对方页面(h1 数=0)。**规矩(硬要求,⛔ 起了专属端口/专属浏览器也不豁免)**:每次采集/测量前**先断言当前标签页 URL 等于预期**(形如 `ASSERT_URL: http://localhost:<你的端口>/<路径>`),断言失败即中止采集重连,⛔ 不带着未断言的读数下任何结论——两次事故都是靠这条拦下的。

**机制层加固(2026-08-17 当晚实施并升级,`ebe6268` 起)**:起窗自动注入**成对**的 `BU_NAME`(verify=`v<cksum>` 短名 / dispatch / lane-a·b)与 `BU_CDP_URL`(**端口段创始人裁定:9230 往上整段归流水线**——dispatch=9230,lane-a/b=9231/9232,verify=9300-9999 按窗口名派生)——**每个窗口一条专属 CDP 通道**(创始人原话),daemon 按名单例、端口预分配,窗口 ⛔ 自挑端口 ⛔ 连共享 Chrome;端口无人监听时 harness 报错是预期 fail-closed。⚠️ **未经两个并发 verify 窗口实测确认前,URL 断言纪律仍是主防线 ⛔ 不撤**。

**Claude 验收窗口 CDP 走查操作段(照抄即用;Codex 侧已熟不需此段)**:

```bash
# 1) 通道是预分配的,先看自己的:
echo "$BU_NAME $BU_CDP_URL"          # 端口取 URL 末段
PORT="${BU_CDP_URL##*:}"
# 2) 用该端口起自己的 headless Chrome(独立临时 profile):
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --remote-debugging-port="$PORT" \
  --user-data-dir="$(mktemp -d)" --no-first-run about:blank &
sleep 2 && curl -s "http://127.0.0.1:$PORT/json/version" | head -1   # 有 JSON 回显=通道就绪
# 3) browser-harness 自动读环境里的 BU_NAME/BU_CDP_URL,直接用:
browser-harness <<'PY'
new_tab("http://localhost:<你的应用端口>/<路径>")
wait_for_load()
print(page_info())   # 采集前先断言 URL(上节硬规则)
PY
# 4) 验收收尾:杀掉自己的 Chrome,别留孤儿进程(创始人:用完要关):
pkill -f "remote-debugging-port=$PORT"
```

`vdown`/`down`/`fresh`/`halt` 回收窗口时会**机器兜底清扫同端口的 headless**(`cdp_sweep`)——兜底存在不豁免第 4 步,自己关是本分。

细节疑难查 browser-harness 官方 interaction-skills/connection.md;⛔ 不设 BU_NAME 单给 BU_CDP_URL(daemon 按名单例,会命中别人的默认 daemon——本节案发机理)。

## 已知边界

- **部署窗口的瞬时假语法错误**(2026-08-19 07:17 实撞,中继判因):11B 清账批落盘期间,正在运行的 `laixin-lane` 一次性命令可能在**主流程成功之后**尾部抛 `syntax error`——`~/.local/bin/laixin-lane` 是指向仓库工作树的软链,运行中脚本被就地改写,bash 续读偏移落进新内容中间(与 loop_stale 要防的「常驻循环驻内存跑旧代」同根:运行中的代与盘上的文件不同代)。**判读口径**:①核主流程产物(窗口起没起/看板记没记)定损失;②之后任意一次调用正常=当前文件健康;③零损失即不处置,⛔ 据此回滚或重跑主流程,⛔ 先怀疑参数(如片名内容)——非部署时段再现才查参数。改完 laixin-lane 记得对常驻循环 stop/start 换代(doctor 会提示);部署原子替换候选已登 11B #25;
- **起窗被专线自检拦截**(2026-08-13 实撞:readiness FAIL → claude 拒启 → 验收窗口没起来):`verify`/`dispatch` 现在会**秒级报出 FAIL 项并记看板**,不再哑等 90s。处置:①这是 fail-closed 保护(封号风险>停摆),**绝不绕过**;②修专线问题(`us-status` / `bash dns-guard.sh`),修好重试;③方向不明升级创始人。夜间流水线因此停摆是**正确行为**,早晨看板会有 ⛔ 记录;
- lane 里 Codex 崩了:`peek` 见 shell 提示符 → `fresh` 重启 → 重发当前片路径(Codex 从分支现场续);
- 迁移片验收必跑回环;B 轨零迁移核 `git diff main --stat`;
- prompt 写死的 worktree 号会失效,**发车前校准**;片合并/验收结束后即回收 worktree(`git worktree remove`)——**真实理由是解发车阻塞,不是省磁盘**:git 不允许两棵树 checkout 同一分支,空闲验收树占着 `main` 会卡住主树 `checkout main`(2026-08-13 实撞);
- 本卡不复制红线/术语/机制——照 kickoff/acceptance 卡指路去索引表。
